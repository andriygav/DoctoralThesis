\documentclass{dissert}
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english,russian]{babel}

\usepackage{fullpage}
\usepackage{lastpage}
\usepackage{enumerate}

\usepackage[all]{xy}
\usepackage{xcolor}
\definecolor{darkgreen}{rgb}{0.0, 0.2, 0.13}
\definecolor{darkcyan}{rgb}{0.0, 0.55, 0.55}

\usepackage{geometry}
\geometry{left=2.5cm}
\geometry{right=1.0cm}
\geometry{top=2.0cm}
\geometry{bottom=2.0cm}
\renewcommand{\baselinestretch}{1.0}

\newcommand{\paragraph}[1]{\noindent\textbf{#1}\quad}

\usepackage{totcount}
\newtotcounter{citnum} %From the package documentation
\def\oldbibitem{} \let\oldbibitem=\bibitem
\def\bibitem{\stepcounter{citnum}\oldbibitem}

\makeatletter
\long\def\@makecaption#1#2{%
  \vskip\abovecaptionskip
  \sbox\@tempboxa{#1.~#2}%
  \ifdim \wd\@tempboxa >\hsize
    #1.~#2\par
  \else
    \global \@minipagefalse
    \hb@xt@\hsize{\hfil\box\@tempboxa\hfil}%
  \fi
  \vskip\belowcaptionskip}
\makeatother

\reversemarginpar

\renewcommand{\contentsname}{Содержание}
\renewcommand{\contentsdesc}{Стр.}
\renewcommand{\chaptername}{Глава}

\makeatletter
\bibliographystyle{utf8gost71u}     % Оформляем библиографию по ГОСТ 7.1 (ГОСТ Р 7.0.11-2011, 5.6.7)
\makeatother

\usepackage{amsthm}
\usepackage{graphicx}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{mathrsfs}
\usepackage{subfig}
\usepackage{caption}
\usepackage{booktabs}
\usepackage{color}
\usepackage{bm}
\usepackage{tabularx}
\usepackage{url}
\usepackage{multirow}
\graphicspath{{../thesis/}}

\newtheorem{theorem}{Теорема}
\newtheorem{lemma}[theorem]{Лемма}
\newtheorem{definition}{Определение}
\newtheorem{corollary}[theorem]{Следствие}
\newtheorem{proposition}[theorem]{Предложение}

\usepackage{comment}
\usepackage{rotating}

\usepackage{autonum}

\begin{document}

\begin{titlepage}
\begin{flushright}
{На правах рукописи}
\end{flushright}
\vspace{1.5cm}
\begin{center}
{Грабовой Андрей Валериевич}
\par
\vspace{2cm}
\textsc{Ландшафтная мера сложности моделей и данных \\в параметрических моделях машинного обучения}
\par
\vspace{2cm}
{1.2.1~--- Искусственный интеллект и машинное обучение}
\par
\vspace{2cm}
{АВТОРЕФЕРАТ\\
диссертации на соискание ученой степени\\
доктора физико-математических наук}
\end{center}
\par
\vspace{3.5cm}
\begin{center}
{Москва~--- 2026}
\end{center}
\end{titlepage}

\setcounter{page}{2}
\noindent {Работа выполнена в Лаборатории 42 <<Интеллектуального анализа данных>> Института проблем управления им. В.А.Трапезникова РАН.

\vspace{0.1cm}

\vskip1ex\noindent
\begin{tabularx}{\linewidth}{@{}lX@{}}
  Научный консультант: & \textbf{Воронцов Константин Вячеславович}\\
  & доктор физико-математических наук, профессор РАН, Федеральный исследовательский~центр <<Информатика и управление>> Российской академии наук, отдел интеллектуальных систем, ведущий научный сотрудник.
  \\[2pt]
  Официальные оппоненты: & \textbf{\underline{\hspace{10cm}}}\\
  & \underline{\hspace{10cm}}\\[2pt]
  & \textbf{\underline{\hspace{10cm}}}\\
  & \underline{\hspace{10cm}}\\[2pt]
  & \textbf{\underline{\hspace{10cm}}}\\
  & \underline{\hspace{10cm}}\\[2pt]
  Ведущая организация: & \underline{\hspace{10cm}}\\[2pt]
  & \underline{\hspace{10cm}}\\[2pt]
\end{tabularx}
\vskip2ex\noindent

\vspace{0.2cm}
\noindent Защита состоится~\underline{\hspace{1cm}}~\underline{\hspace{3cm}}~\underline{\hspace{1.5cm}}~года~в~14:00 на~заседании диссертационного совета Д 002.073.05 при Федеральном исследовательском центре <<Информатика и управление>> Российской академии наук (ФИЦ~ИУ~РАН) по адресу: 119333, г.\,Москва, ул.\,Вавилова, д.\,40.

\vspace{0.2cm}
\noindent С диссертацией можно ознакомиться в библиотеке Федерального государственного учреждения Федеральный исследовательский центр <<Информатика и управление>> Российской академии наук и на сайте http://www.frccsc.ru/

\vspace{0.2cm}
\noindent Автореферат разослан  \qquad \qquad \qquad \qquad 2026 года.

\vspace{0.3cm}
\noindent Ученый секретарь\\
диссертационного совета Д 002.073.05\\
к.т.н.
\hspace{12cm} И.\,А.\;Рейер
}

\clearpage

\pretolerance=-1

\section*{Общая характеристика работы}
\paragraph{Актуальность темы.}
С начала XXI века производительность вычислительных систем, измеряемая в операциях с плавающей запятой~(англ. FLOPS), выросла экспоненциально: от терафлопсов до экзафлопсов.
Параллельно на несколько порядков выросла и сложность моделей машинного обучения: от тысяч обучаемых параметров в начале 2000-х до миллиардов и сотен миллиардов в 2020-х, причем в ближайшие годы ожидается переход к триллионам параметров.
Анализ сложности таких моделей сегодня в основном опирается на эмпирические методы. К примеру, качество связывают с числом параметров и с вычислительными затратами на обучение.
Без строгой теории, позволяющей предсказывать поведение моделей при масштабировании, разработка оказывается затратной по вычислительным ресурсам, а получаемые результаты~--- часто плохо воспроизводимыми и противоречивыми.

Особенно остро проблема стоит для больших языковых моделей~(англ. LLM), так как их обучение требует больших вычислительных, энергетических и финансовых затрат.
Чтобы уменьшить неопределенность при предобучении~(англ. pretrain), на практике эмпирически подбирают соотношение между числом параметров модели и объемом обучающих данных, измеряемым в токенах, при заданном вычислительном бюджете.
Но оценки, полученные для одной архитектуры, обычно не переносятся на другие, поэтому такие подходы ограничены и при полномасштабном обучении нередко дают неожиданные результаты.
Поэтому, чтобы обоснованно выбрать размер модели и объем данных, нужны теоретические оценки сложности, позволяющие связать сложность модели со сложностью выборки еще на этапе проектирования.

Классические подходы к оценке сложности моделей машинного обучения, разработанные для традиционных методов, находят ограниченное применение при анализе параметрических моделей глубокого обучения.
Фундаментальный вклад в теорию оценивания сложности моделей внесли Владимир Наумович Вапник и Алексей Яковлевич Червоненкис, заложившие основы статистической теории обучения, а также Лесли Вэлиант, предложивший подход приближенно правильного обучения~(англ. PAC-Learning).
Современные подходы к определению сложности в основном опираются на радемахеровскую сложность, предложенную Владимиром Кольчинским и Дмитрием Панченко.
Отдельно стоит отметить комбинаторный подход Константина Вячеславовича Воронцова к оценке обучаемости алгоритмов, в рамках которого разработан математический аппарат на основе комбинаторных оценок вероятности переобучения моделей.
Однако эти исследования не рассматривают нейросетевые модели глубокого обучения ввиду их высокой сложности и трудности получения адекватных оценок.
Значительная часть современных исследований посвящена классическим методам машинного обучения и улучшению оценок для известных методов, так как текущие оценки сложности даже для классических моделей сильно завышены.

Отдельным направлением исследований являются оценки способности моделей глубокого обучения к аппроксимации по прецедентам.
Основополагающий результат принадлежит Джорджу Цибенко, который доказал, что нейронные сети при некоторых ограничениях аппроксимируют непрерывные функции с произвольной точностью.
Йохану Хестаду принадлежат первые оценки, указывающие на рост выразительной способности сетей с их глубиной.
В более поздних работах Яна Лекуна, Йошуа Бенджио, Надава Коэна эти оценки получены с более мягкими ограничениями на класс рассматриваемых функций.
Эти оценки наряду с работой Охада Шамира указывают на экспоненциальное увеличение выразительной способности моделей с ростом числа слоев.

В настоящей работе анализируются нейронные сети через матрицы Гессе.
Матрица Гессе функции потерь по параметрам модели описывает локальную кривизну ландшафта функции потерь.
По ней оцениваются сложность модели и сложность обучающей выборки, а также значимость параметров при прореживании нейронных сетей.
Рассматриваются параметрические модели машинного обучения в постановке минимизации эмпирического риска.
Центральный вопрос исследования состоит в том, согласована ли сложность модели со сложностью выборки, на которой она обучается.

\paragraph{Степень разработанности темы диссертационного исследования.}
Классические оценки сложности~--- VC-размерность, PAC-обучаемость и радемахеровская сложность~--- построены для моделей с небольшим числом параметров и ориентированы на наихудший случай.
Для перепараметризованных нейронных сетей такие оценки обычно слишком консервативны, так как они не учитывают сверточные фильтры, остаточные связи и механизмы внимания и не отражают влияние регуляризации, ранней остановки и хода оптимизации на обобщающую способность.
Комбинаторный подход К.\,В.~Воронцова снижает оценки Вапника--Червоненкиса, но применим лишь к узким классам моделей.

Оценка сложности данных чаще всего сводится к объему выборки, хотя сложность определяется и свойствами отдельных объектов.
Современные методы оценки сложности объектов опираются на анализ многообразий, аппроксимирующих элементы выборки.
Для больших языковых моделей широко используют эмпирические законы масштабирования Каплана и Хофмана, однако эти законы не имеют строгого теоретического обоснования и не объясняют механизмы связи между параметрами, данными и качеством модели.

Для снижения сложности моделей применяют квантизацию, прореживание и дистилляцию знаний.
Эти методы не опираются на строгие оценки сложности модели и данных, поэтому их эффективность и предсказуемость ограничены.

В итоге единого теоретического аппарата до сих пор нет. Он давал бы асимптотические оценки сложности параметрических моделей и формализовал бы согласованность модели с обучающей выборкой.
Такой аппарат нужен, чтобы сравнивать параметрические модели и выбирать архитектуру под задачу с выборкой данной сложности.

\paragraph{Объектом исследования} являются процессы обучения параметрических моделей машинного обучения по конечным выборкам.

\paragraph{Предмет исследования:} согласованность сложности параметрических моделей машинного обучения с характеристиками обучающей выборки, формализуемая мерами на модели и на выборке.

\paragraph{Цель и задачи исследования.}
Целью исследования является построение теоретического аппарата оценки согласованности сложности параметрических моделей машинного обучения с характеристиками обучающей выборки и методов снижения сложности таких моделей на основе анализа ландшафта функции потерь.

Для достижения цели в работе поставлены и решены следующие задачи:
\begin{itemize}
    \item[--] введение определений мер сложности моделей и выборок в рамках теории мер и формулировка критерия согласованности модели и выборки;
    \item[--] построение ландшафтной меры сложности модели по матрицам Гессе и установление ее связи с приращением эмпирического риска при изменении выборки;
    \item[--] получение теоретических оценок ландшафтной меры для полносвязных, сверточных и трансформерных архитектур;
    \item[--] построение методов оценки достаточного объема выборки и анализ их связи со сложностью модели через условную сложность выборки и стабилизацию эмпирического риска;
    \item[--] построение методов снижения сложности моделей, опирающихся на локальную геометрию функции потерь;
    \item[--] демонстрация применимости построенного аппарата в прикладных задачах.
\end{itemize}

\paragraph{Методы исследования.}
В работе используются теория мер, линейная алгебра и матричный анализ, включая матричное дифференцирование и спектральный анализ, теория вероятностей и математическая статистика, включая байесовский подход и сходимость случайных процессов, теория оптимизации, анализ функций многих переменных и статистическая теория обучения.

\paragraph{Научная новизна.}
\begin{enumerate}
    \item Введены меры сложности выборки $\mu_D(D)$ и модели $\mu_f(f)$ в терминах теории мер и сформулирован критерий согласованности $\mu_f(f) \le \mu_D(D)$.
    \item Введена ландшафтная мера сложности модели
    \[
    \mu_f(f|D) = \mathbb{E}_{i}\big\|\mathbf{H}_{i}(\boldsymbol{\theta}^*)-\mathbb{E}_{j}\mathbf{H}_{j}(\boldsymbol{\theta}^*)\big\|_2,
    \]
    где ожидания берутся по равномерному выбору объектов $i,j$ из выборки~$D$, а $\|\cdot\|_2$~--- спектральная норма. Мера количественно связывает изменение выборки с локальной геометрией ландшафта функции потерь.
    \item Для полносвязных, сверточных и трансформерных архитектур получены оценки ландшафтной меры через спектральные нормы матриц Гессе. Оценки растут экспоненциально по глубине и полиномиально по ширине и числу каналов.
    \item Для линейной регрессии предложены байесовские критерии достаточного объема выборки по устойчивости правдоподобия и близости апостериорных распределений параметров, а также доказана сходимость моментов апостериорных распределений. Связь со сложностью модели проводится через условную сложность выборки и стабилизацию эмпирического риска.
    \item Предложены методы снижения сложности, опирающиеся на локальную геометрию функции потерь, а именно прореживание по ковариации градиентов, многодоменная дистилляция и антидистилляция.
    \item Для низкоранговой адаптации параметров доказана состоятельность оценки при стандартных условиях. Для многозадачного обучения сравнены верхние оценки эмпирической радемахеровской сложности на задачу при справедливом масштабировании нормы параметров энкодера.
\end{enumerate}

\paragraph{Теоретическая значимость работы.}
Результаты работы вписывают локальный анализ матриц Гессе в теорию обучаемости.
Этот анализ связывает согласованность модели и выборки с архитектурой модели и дополняет классические меры сложности, основанные на VC-размерности, PAC-обучаемости и радемахеровской сложности.
Он позволяет распространить задачу согласования модели с выборкой на перепараметризованные нейронные сети, для которых классические подходы слишком консервативны.
Полученные оценки ландшафтной меры дают основание для сравнительного анализа архитектур между собой и для сопоставления методов оценки достаточного объема выборки со сложностью модели.
Основной вклад работы состоит в построении ландшафтной меры сложности параметрической модели.
На ее основе устанавливается вычислимая связь между архитектурой модели, стабилизацией обучения и характеристиками выборки.

\paragraph{Практическая значимость работы.}
Разработанный теоретический аппарат применен к решению прикладных задач машинного обучения: многозадачного обучения, декодирования фМРТ-изображений и детекции машинно-генерированного контента.
Полученные методы оценки и снижения сложности моделей и данных экспериментально проверены на реальных задачах компьютерного зрения, обработки естественного языка и классификации.

\paragraph{Положения, выносимые на защиту:}
\begin{enumerate}
    \item Меры сложности модели и выборки вместе с критерием их согласованности задают формальный аппарат соответствия параметрической модели машинного обучения характеристикам обучающей выборки.
    \item Ландшафтная мера на основе спектральных свойств матриц Гессе количественно связывает архитектуру модели с изменением эмпирического риска при изменении выборки.
    \item Оценки ландшафтной меры растут экспоненциально с глубиной сети и полиномиально с шириной и числом каналов.
    \item Критерии достаточного объема выборки по близости апостериорных распределений сходятся в модели линейной регрессии.
    \item Снижение размерности пространства обучаемых параметров уменьшает число параметров модели при сохранении порядка качества аппроксимации.
\end{enumerate}
Ключевые математические результаты, соответствующие указанным положениям, в основном содержании автореферата приведены в виде формулировок теорем без доказательств.

\paragraph{Степень достоверности результатов.}
Достоверность научных результатов работы подтверждается непротиворечивостью и согласованностью с известными фактами и исследованиями в рассматриваемой области, высокой степенью сходимости теоретических результатов с данными экспериментов и определяется применением теоретических и методологических основ разработок ведущих ученых в области обработки естественного языка, корректным и обоснованным использованием математического аппарата, экспериментальными исследованиями разработанных моделей и методов.

\paragraph{Соответствие диссертации паспорту специальности.}
Тема и основные результаты диссертации соответствуют следующим областям исследований паспорта специальности 1.2.1~--- Искусственный интеллект и машинное обучение.

2 Исследования в области оценки качества и эффективности алгоритмических и программных решений для систем искусственного интеллекта и машинного обучения. Методики сравнения и выбора алгоритмических и программных решений при многих критериях.

4 Разработка методов, алгоритмов и создание систем искусственного интеллекта и машинного обучения для обработки и анализа текстов на естественном языке, для изображений, речи, биомедицины и других специальных видов данных.

16 Исследования в области специальных методов оптимизации, проблем сложности и элиминации перебора, снижения размерности.

17 Исследования в области многослойных алгоритмических конструкций, в том числе – многослойных нейросетей.

\paragraph{Апробация результатов диссертации.}
Основные результаты работы докладывались и обсуждались на Всероссийской конференции с международным участием «Математические методы распознавания образов»~(Москва, 2019, Москва, 2021, Муром, 2025), Международной конференции «Интеллектуализация обработки информации»~(Гаэта, 2018, Москва, 2020, Москва, 2022), Всероссийской научной конференции МФТИ~(Москва, 2018, 2019, 2020, 2021, 2023, 2024, 2025, 2026), VI Международная научно-практическая конференция «Магия ИННО: Перспективы развития лингвистики и лингводидактики в современных условиях»~(Москва, 2023), Ivannikov Ispras Open Conference~(Москва, 2021, 2022, 2023, 2024, 2025), Ivannikov Memorial Workshop~(Казань, 2022, Этномир, 2026), Iberian Languages Evaluation Forum co-located with the Conference of the Spanish Society for Natural Language Processing~(Андалусия, 2023, 2024), 11-я Международная научно-практическая конференция «Научное издание международного уровня – 2023: достижения, реалии, перспективы»~(Москва, 2023), 35th Conference of Open Innovations Association~(Тампере, 2024), Fourth Workshop on Scholarly Document Processing~(Бангкок, 2024), 1st Workshop on GenAI Content Detection (GenAIDetect)~(Абу-Даби 2025), 19th International Workshop on Semantic Evaluation~(Вена, 2025).

\paragraph{Публикации.}
По теме диссертации опубликовано 66 научных работ, из
которых 25 статей в научно-технических журналах, входящих в перечень ВАК, 30~--- в изданиях, входящих в международные наукометрические базы Scopus и Web of Science.
В трудах российских и международных конференций опубликовано 39 работ. Также на основе работ автора зарегистрировано 13 программ для ЭВМ.

\paragraph{Личный вклад соискателя.}
Все выносимые на защиту результаты и положения, составляющие основное содержание диссертационного исследования, разработаны и получены лично автором или при его непосредственном участии вместе с учениками.
В работах, опубликованных в соавторстве, соискателю принадлежит определяющая роль в построении теоретических методов и направлении исследований.

\paragraph{Структура и объем работы.}
Диссертация состоит из оглавления, введения, шести разделов, заключения, списка иллюстраций, списка таблиц, перечня основных обозначений и списка литературы из~$262$ наименования. Основной текст занимает~$364$ страницы.

\section*{Основное содержание работы}
Во \textbf{введении} обоснована актуальность темы диссертации,
сформулирована цель исследования, указаны основные задачи и научные положения, выносимые на защиту, охарактеризованы научная новизна, теоретическая и практическая значимость, указаны используемые методы исследования.

В \textbf{первой главе} дан обзор теории обучаемости и классических мер сложности моделей. Глава носит методический характер и мотивирует переход к локальному анализу ландшафта функции потерь.

В \textbf{разделе 1.1} введены обозначения постановки статистического обучения: выборка $D=\{(\mathbf{x}_i,\mathbf{y}_i)\}_{i=1}^m$ из неизвестного распределения $\mathbb{P}_{\mathbf{X},\mathbf{Y}}$, класс моделей $\mathfrak{F}$ и функция потерь $\ell$.
Истинный риск $R(f)=\mathbb{E}[\ell(f(\mathbf{X}),\mathbf{Y})]$ противопоставляется эмпирическому риску $\hat{R}_m(f)$ по выборке.
Обучение рассматривается как минимизация эмпирического риска.
Центральным объектом классической теории является отклонение $|R(f)-\hat{R}_m(f)|$ и его равномерная оценка по классу $\mathfrak{F}$.

В \textbf{разделе 1.2} кратко обсуждаются классические меры емкости класса: VC-размерность, PAC-обучаемость и комбинаторные оценки К.В.~Воронцова.
Для перепараметризованных нейронных сетей эти подходы дают слишком консервативные оценки наихудшего случая и не учитывают архитектуру, регуляризацию и локальный режим оптимизации.
В дальнейшем используется радемахеровская сложность, поскольку она применяется при сопоставлении с ландшафтной мерой и в анализе многозадачного обучения.

\begin{definition}[Эмпирическая радемахеровская сложность]
    Пусть $\mathcal{F}$~--- класс вещественных функций на $\mathcal{X}$, $D = \{\mathbf{x}_1,\ldots,\mathbf{x}_m\} \subset \mathcal{X}$~--- конечная выборка, и $\sigma_1,\ldots,\sigma_m$~--- независимые случайные переменные Радемахера, то есть $\mathbb{P}\{\sigma_i = +1\} = \mathbb{P}\{\sigma_i = -1\} = 1/2$ для всех $i = 1,\ldots,m$. \textit{Эмпирическая радемахеровская сложность} класса $\mathcal{F}$ на выборке $D$ определяется как
    \[
        \hat{\mathfrak{R}}_D(\mathcal{F}) = \mathbb{E}_{\sigma}\left[\sup_{f \in \mathcal{F}} \frac{1}{m} \sum_{i=1}^{m} \sigma_i f(\mathbf{x}_i)\right],
    \]
    где математическое ожидание берется по распределению случайных переменных Радемахера.
\end{definition}

\begin{definition}[Ожидаемая радемахеровская сложность]
    \textit{Ожидаемая радемахеровская сложность} класса $\mathcal{F}$ для выборок размера $m$ определяется как
    \[
        \mathfrak{R}_m(\mathcal{F}) = \mathbb{E}_{D \sim \mathbb{P}^m}\left[\hat{\mathfrak{R}}_D(\mathcal{F})\right],
    \]
    где математическое ожидание берется по распределению выборок $D$ размера $m$, порожденных распределением $\mathbb{P}$ на $\mathcal{X}$.
\end{definition}

Радемахеровская сложность задает равномерную емкость класса на конкретной выборке и входит в стандартные границы обобщения вида $R(f)\le\hat{R}_m(f)+2\hat{\mathfrak{R}}_D(\mathcal{F})+O\!\bigl(\sqrt{\log(1/\delta)/m}\bigr)$.
Вместе с тем она не различает две модели одной архитектуры, оказавшиеся в разных областях параметрического пространства, и поэтому недостаточна как единственная мера сложности глубокой сети.

В \textbf{разделе 1.3} рассматриваются выразительная способность глубоких сетей и анализ ландшафта функции потерь через матрицы Гессе.
Показано, что качество модели зависит не только от числа параметров, но и от области параметрического пространства, в которую приводит оптимизация.
Спектр матрицы Гессе обычно имеет большинство собственных значений около нуля и лишь несколько доминирующих направлений кривизны, а это указывает на необходимость локального описания сложности, а не только глобальной емкости класса.
Отсюда переход от емкости класса к разбросу локальных вторых производных функции потерь.

\begin{figure}[h!]
\centering
\includegraphics[width=0.72\linewidth]{figures/chapter-1/losses_difference.pdf}
\caption{Идея анализа сходимости ландшафта: разность $|\mathcal{L}_{k+1}(\boldsymbol{\theta})-\mathcal{L}_k(\boldsymbol{\theta})|$ при добавлении объекта уменьшается с ростом объема выборки~$k$. На этой идее строится ландшафтная мера сложности модели.}
\label{autoref:fig:losses-difference}
\end{figure}

В \textbf{разделе 1.4} обсуждаются эмпирические законы масштабирования языковых моделей и методы снижения сложности: прореживание, квантизация, дистилляция и низкоранговые адаптеры.
Соотношения между числом параметров, объемом данных и качеством полезны при практическом использовании, но не дают теоретического критерия согласованности архитектуры и выборки.
Методы сжатия и адаптации обычно тоже не опираются на строгие оценки такой согласованности, поэтому их предсказуемость ограничена.

В \textbf{разделе 1.5} фиксируются пробелы, на которые отвечает диссертация: нет вычислимой меры сложности, учитывающей архитектуру и локальный оптимум, классические оценки слабо связаны с ростом выборки, теоретические оценки спектральных норм матриц Гессе для полносвязных, сверточных и трансформерных моделей недостаточны.
Предлагаемый аппарат связывает меры $\mu_D$, $\mu_f$ и ландшафтную меру $\mu_f(f|D)$ с оценками матриц Гессе и методами снижения сложности.

Таким образом, первая глава задает контекст теории обучаемости и обосновывает переход к локальной геометрии функции потерь в последующих главах.

Во \textbf{второй главе} разрабатывается теоретический формализм, в котором общие меры сложности модели и данных вводятся как исходная формализация задачи, а основной вычислимый объект задается ландшафтной мерой параметрической модели.

В \textbf{разделе 2.1} вводятся формальные определения мер сложности в рамках теории мер.
Множество конечных выборок рассматривается как алгебра подмножеств генеральной совокупности, что позволяет применять стандартные свойства конечных мер.
Для меры сложности выборки требуется субаддитивность:
\[
    \mu_D(D_1\cup D_2)\le \mu_D(D_1)+\mu_D(D_2).
\]
Это свойство отражает естественное требование: объединение двух наборов данных не должно иметь сложность больше суммы их сложностей.
При непересекающихся выборках равенство соответствует аддитивному случаю, когда объекты не дублируют информацию друг о друге.
Монотонность меры сложности конечных выборок используется как следствие субаддитивности и не требует дополнительных предположений в рассматриваемой конечной постановке.

\begin{definition}[Генеральная совокупность данных]
    Генеральной совокупностью данных~$\Gamma$ назовем произвольное множество объектов, которые исследуются в рамках той или иной задачи. В общем случае нет никаких ограничений на счетность множества генеральной совокупности.
\end{definition}

\begin{definition}[Однородная и гетерогенная генеральная совокупность]
    Генеральную совокупность~$\Gamma$ назовем однородной, если все объекты генеральной совокупности порождаются из одного распределения. В противном случае генеральную совокупность назовем гетерогенной. Если совокупность образуется $k$ различными распределениями, будем говорить о $k$-компонентной совокупности.
\end{definition}

Пусть задана генеральная совокупность данных~$\Gamma$.
Множество всех подмножеств объектов, образующих кольцо выборок, обозначим как:
\[
    \mathfrak{D} = \{D_\Gamma^i\}, \quad D_\Gamma^i \subset \Gamma.
\]

\begin{definition}[Мера сложности выборки]
    Мерой сложности выборки назовем отображение~$\mu_D,$ такое, что:
    \[
        \mu_D(D_i) : \mathfrak{D}_\Gamma \to \mathbb{R}_+,
    \]
    удовлетворяющее свойству:
    \[
        \mu_D(D_i\cup D_j) \leq \mu_D(D_i)+\mu_D(D_j),
    \]
    где равенство достигается при условии~$D_i\cap D_j=\emptyset.$
\end{definition}

\begin{lemma}[Монотонность меры сложности выборки]
    Мера $\mu_D$ монотонна: если $D_1 \subseteq D_2$, то
    \[
        \mu_D(D_1) \leq \mu_D(D_2).
    \]
\end{lemma}

Пусть задано множество параметрических моделей $\mathfrak{F}=\{f_i\}$.

\begin{definition}[Мера сложности модели]
    Мера сложности модели~$f$~--- отображение
    \[
        \mu_f(f_i) : \mathfrak{F} \to \mathbb{R}_+.
    \]
\end{definition}
В отличие от классических мер емкости класса функций, $\mu_f(f)$ относится к конкретной модели, то есть к конкретному элементу параметрического семейства.
Для глубоких сетей это существенно: две модели одной архитектуры оказываются в разных областях параметрического пространства и имеют разную локальную геометрию функции потерь.
Поэтому $\mu_f$ далее конкретизируется через матрицы Гессе в окрестности опорной точки.

\begin{definition}[Критерий согласованности модели и выборки]
    Модель $f\in\mathfrak{F}$ назовем \textit{согласованной} с выборкой $D\in\mathfrak{D}$, если
    \[
        \mu_f(f)\leq \mu_D(D).
    \]
\end{definition}

Это абстрактный критерий согласованности числовых характеристик сложности модели и выборки.
Сам по себе он не задает качество аппроксимации, но на его основе вводятся вычислимые частные меры.

\begin{proposition}[Сохранение критерия согласованности при расширении выборки]
    Пусть $\mu_D$ монотонна по включению: из $D \subseteq D''$ следует $\mu_D(D) \leq \mu_D(D'')$.
    Если $\mu_f(f) \leq \mu_D(D)$, то для любой выборки $D'$ верно $\mu_f(f) \leq \mu_D(D \cup D')$.
\end{proposition}
Субаддитивность $\mu_D(D\cup D')\le \mu_D(D)+\mu_D(D')$ дает верхнюю оценку сложности объединения и используется в дальнейших рассуждениях главы.

В \textbf{разделе 2.2} сложность данных ставится в зависимость от модели.
Условная сложность выборки~$D$ относительно~$f$:
\begin{definition}[Условная сложность выборки]\label{def:conditional-complexity}
\[
    \mu_D(D|f)
    =
    \inf \{ \mu_D(D') : D' \subseteq D,\ \mu_f(f) \leq \mu_D(D') \},
    \qquad
    \inf\varnothing=+\infty.
\]
\end{definition}
Согласно определению~\ref{def:conditional-complexity}, величина $\mu_D(D|f)$ есть минимальная сложность подвыборки $D'\subseteq D$, при которой еще выполнен критерий согласованности $\mu_f(f)\le\mu_D(D')$.
Отсюда, одна и та же~$D$ является простой для одной модели и сложной для другой, а $\mu_D(D)$ и вычислимая ландшафтная мера $\mu_f(f|D)$~--- разные объекты.

\begin{definition}[Простая генеральная совокупность]
Однородная генеральная совокупность~$\Gamma_C$ проста, если каждому объекту приписана одна и та же сложность~$C$.
\end{definition}
\begin{corollary}
Для~$D\subset\Gamma_C$
\[
    \mu_D(D)=C\cdot|D|.
\]
\end{corollary}
В этом случае
\[
    \mu_D(D|f)=C\cdot m^*(f),\qquad
    m^*(f)=\min\bigl\{|D'|: D'\subseteq D,\ \mu_f(f)\le C|D'|\bigr\},
\]
и достаточный размер выборки~$m^*$~--- частный случай условной сложности выборки, рассматриваемый далее в главе~4.

\begin{definition}[Достаточный размер выборки]
Размер~$m^*$ достаточен по критерию~$T$, если $T$ выполняется для всех $k\geqslant m^*$.
\end{definition}

В \textbf{разделе 2.3} разрабатывается ландшафтная мера сложности модели, основанная на анализе сходимости функции потерь при увеличении объема выборки.
Рассматривается параметрическая модель $f_{\boldsymbol\theta}$ и эмпирический риск $\mathcal{L}_k(\boldsymbol\theta)$.
При добавлении нового объекта изменение функции потерь имеет вид
\[
    \mathcal{L}_{k+1}(\boldsymbol\theta)-\mathcal{L}_k(\boldsymbol\theta)=\frac1{k+1}\bigl(\ell(f_{\boldsymbol\theta}(\mathbf{x}_{k+1}),\mathbf{y}_{k+1})-\mathcal{L}_k(\boldsymbol\theta)\bigr).
\]

В предположении о сохранении локального минимума и с использованием квадратичного приближения Тейлора получена оценка
\[
    |\mathcal{L}_{k+1}(\boldsymbol\theta)-\mathcal{L}_k(\boldsymbol\theta)|\le\frac{M_\ell}{k+1}+\frac{\|\boldsymbol\theta-\boldsymbol\theta^*\|_2^2}{2(k+1)}
    \Bigl\|\mathbf{H}_{k+1}(\boldsymbol\theta^*)-\frac1k\sum_{i=1}^k\mathbf{H}_i(\boldsymbol\theta^*)\Bigr\|_2,
\]
где $\mathbf{H}_i(\boldsymbol\theta)$ — матрица Гессе для $i$-го объекта.

Оценка изменения ландшафта задает свойство модели~$f$ на выборке~$D$, конкретизируемое как условная сложность
\[
    \mu_f(f|D) : \mathfrak{F} \to \mathbb{R}_+.
\]
Частным случаем такого отображения служит ландшафтная мера сложности, определяемая в этом разделе через разброс пообъектных матриц Гессе.

\begin{definition}[Условная сложность параметрической модели]
    Условная сложность модели~$f$ относительно выборки~$D$~--- отображение $\mu_f(f|D):\mathfrak{F}\to\mathbb{R}_+$.
\end{definition}

\begin{definition}[Ландшафтная мера сложности]
    Ландшафтная мера сложности параметрической функции~$f$~--- условная сложность
    \begin{equation}\label{ch1lc:eq:muf}
        \mu_f(f|D) = \mathsf{E}_{\mathbf{x}_i\in D}\left\| \mathbf{H}_{i}(\boldsymbol{\theta}^*) -  \mathsf{E}_{\mathbf{x}_i\in D}\mathbf{H}_{i}(\boldsymbol{\theta}^*) \right\|_2.
    \end{equation}
\end{definition}

Из основной оценки следует
\[
    |\mathcal{L}_{k+1}(\boldsymbol\theta)-\mathcal{L}_k(\boldsymbol\theta)|\le\frac{M_\ell}{k+1}+\frac{\mu_f(f|D)}{k+1}\|\boldsymbol\theta-\boldsymbol\theta^*\|_2^2.
\]

Для простой генеральной совокупности ($\mu_D(D')=C|D'|$) оценка ландшафтного приращения связывает размер подвыборки с допустимым изменением эмпирического риска.
Если в $R$-окрестности опорной точки требуется
\[
    |\mathcal{L}_{k+1}(\boldsymbol\theta)-\mathcal{L}_k(\boldsymbol\theta)|\le\varepsilon,
\]
то достаточно
\[
    k+1\ge\frac{M_\ell+\mu_f(f|D)R^2}{\varepsilon}.
\]
Таким образом, общая постановка меры сложности данных связывается с ландшафтным анализом модели и не претендует на универсальное описание сложности данных.
В итоге достаточный объем выборки определяется через допустимое изменение эмпирического риска около опорной точки.
При большой ландшафтной мере добавление объектов сильнее меняет локальную геометрию, и для стабилизации нужно больше данных, причем при малом разбросе пообъектных матриц Гессе эмпирический риск при росте выборки меняется устойчивее.

Далее на основе результатов о спектральных нормах матриц Гессе получены оценки ландшафтной меры для конкретных архитектур.

В \textbf{подразделе 2.3.1} для полносвязных сетей доказана теорема.

\begin{theorem}[Сходимость ландшафта функции потерь для полносвязных сетей]
Пусть параметры $\boldsymbol{\theta}$ удовлетворяют $\left\|\boldsymbol{\theta} - \boldsymbol{\theta}^*\right\|_2^2 \leqslant R^2$ для некоторого $R > 0$. Если существует $M_{\ell}\ge 0$ такая, что $\left| \ell(f_{\boldsymbol{\theta}^*}(\mathbf{x}_{i}), \mathbf{y}_{i}) \right| \leqslant M_{\ell}$ для всех $i = 1, \ldots, m$, $\|\mathbf{W}^{(p)}\|_2\le M_{\mathbf{W}}$ для всех слоев и $\|\mathbf{x}_i\|_2\le M_{\mathbf{x}}$ для всех $i$, то
    \begin{align}
        \left| \mathcal{L}_{k+1}(\boldsymbol{\theta}) - \mathcal{L}_k(\boldsymbol{\theta}) \right| \leqslant \frac{1}{k+1}\left( 2M_{\ell} + \left( L \sqrt{2} M_{\mathbf{x}}^2 M_{\mathbf{W}}^{2L} + \sqrt{2} \frac{M_{\mathbf{W}}^2 (M_{\mathbf{W}}^{2L} - 1)}{M_{\mathbf{W}}^2 - 1} \right) R^2 \right),
    \end{align}
    и при $k\to\infty$
    \begin{align}
        \left| \mathcal{L}_{k+1}(\boldsymbol{\theta}) - \mathcal{L}_k(\boldsymbol{\theta}) \right| \to 0.
    \end{align}
    В итоге
    \begin{equation}
        \left| \mathcal{L}_{k+1}(\boldsymbol{\theta}) - \mathcal{L}_k(\boldsymbol{\theta}) \right| \propto \frac{L (hM)^{2L} R^2}{k}.
    \end{equation}
\end{theorem}
Оценка показывает, что глубина входит в сложность сильнее ширины: рост числа слоев дает экспоненциальный вклад в верхнюю оценку, а ширина~--- полиномиальный через размер скрытого слоя.
Это согласуется с известными оценками выразительной способности глубоких сетей, но описывает не аппроксимацию, а локальную чувствительность функции потерь к изменению выборки.

\begin{corollary}[Асимптотика ландшафтной меры для полносвязных сетей]
Ландшафтная мера сложности параметрической функции~$f$ полносвязной нейросетевой модели глубокого обучения имеет асимптотику:
    \[
        \mu_f(f|D) \propto L(hM)^{2L},
    \]
    где~$M$~--- некоторая константа, ограничивающая параметры и данные.
\end{corollary}

В регрессионной постановке с квадратичной функцией потерь и FC ReLU-сетями получена более точная локальная оценка.
Пообъектная квадратичная функция потерь имеет вид
\begin{equation}\label{ch1lc:eq:loss-i}
  \ell_i(\boldsymbol{\theta})
  = \ell\bigl(f_{\boldsymbol{\theta}}(\mathbf{x}_i),\mathbf{y}_i\bigr)
  = \tfrac{1}{2}\bigl(f_{\boldsymbol{\theta}}(\mathbf{x}_i)-y_i\bigr)^2,
  \qquad \mathbf{y}_i = y_i\in\mathbb{R}.
\end{equation}
Следующая лемма задает константу~$M_{\mathbf{H}}$ для полносвязных ReLU-сетей без смещений при спектральных ограничениях на веса и входы.

\begin{lemma}[Спектральная норма пообъектной матрицы Гессе для квадратичной потери и FC-сетей]\label{ch1lc:lem:hess-mse}
Рассмотрим полносвязную ReLU-сеть без смещений, для которой
$\|\mathbf{W}^{(p)}\|_2 \le M_{\mathbf{W}}$, $\|\mathbf{x}_i\|_2 \le M_{\mathbf{x}}$ для всех $p$ и $i$.
Тогда существует константа $M_{\mathbf{H}}$ такая, что для каждого $i$ и каждой точки параметрического пространства, где требуемые производные существуют,
\begin{equation}\label{ch1lc:eq:Mh-bound}
  \bigl\| \mathbf{H}_i(\boldsymbol{\theta}) \bigr\|_2 \le M_{\mathbf{H}}
  := L\, M_{\mathbf{x}}^2 M_{\mathbf{W}}^{2L}
  + \sum_{s=1}^{L} M_{\mathbf{W}}^{2s} .
\end{equation}
Если $|w_{ij}^{(p)}|\le M$ на блоках размера $h\times h$, то $M_{\mathbf{W}}\le hM$ и асимптотически
$\|\mathbf{H}_i(\boldsymbol{\theta})\|_2 = O\!\bigl(L(hM)^{2L}\bigr)$.
\end{lemma}

\begin{theorem}[Локальная оценка ландшафтного приращения для квадратичной потери и FC-сетей]\label{ch1lc:thm:landscape-mse}
Пусть $\boldsymbol{\theta}$ удовлетворяет $\|\boldsymbol{\theta}-\boldsymbol{\theta}^*\|_2\le R$.
Предположим, что $\bigl|\ell(f_{\boldsymbol{\theta}^*}(\mathbf{x}_i),\mathbf{y}_i)\bigr|\le M_{\ell}$ для всех $i$, лемма~\ref{ch1lc:lem:hess-mse} применима в точке $\boldsymbol{\theta}^*$, а в опорной точке выполнено условие приближенной стационарности: существует $\varepsilon_g\ge 0$ такое, что
\[
  \bigl\|\nabla \mathcal{L}_k(\boldsymbol{\theta}^*)\bigr\|_2 \le \varepsilon_g,
  \qquad
  \bigl\|\nabla \mathcal{L}_{k+1}(\boldsymbol{\theta}^*)\bigr\|_2 \le \varepsilon_g .
\]
Тогда
\begin{equation}
  \bigl|\mathcal{L}_{k+1}(\boldsymbol{\theta}) - \mathcal{L}_k(\boldsymbol{\theta})\bigr|
  \le \frac{2}{k+1}\bigl( M_{\ell} + M_{\mathbf{H}} R^2 \bigr) + 2\,\varepsilon_g\, R .
\end{equation}
Это оценка наихудшего случая, полученная из равномерного контроля матриц Гессе. С учетом асимптотики $M_{\mathbf{H}}$ вклад кривизны имеет порядок
\begin{equation}
  \bigl|\mathcal{L}_{k+1}(\boldsymbol{\theta}) - \mathcal{L}_k(\boldsymbol{\theta})\bigr|
  \;=\; O\!\left(\frac{L(hM)^{2L} R^2}{k}\right).
\end{equation}

Далее фиксируется уточнение, учитывающее величину остатков.
Если, кроме того,
\[
  \bigl|f_{\boldsymbol{\theta}^*}(\mathbf{x}_i)-y_i\bigr|\le \varepsilon_y,
  \qquad i=1,\dots,m,
\]
и каждый наблюдаемый вход остается в фиксированной области активации в окрестности $\boldsymbol{\theta}^*$, зададим
\[
  \mathbf{g}_i:=\nabla_{\boldsymbol{\theta}}f_{\boldsymbol{\theta}^*}(\mathbf{x}_i),
  \qquad
  M_{\nabla^2 f}:=
  \max_{1\le i\le m}\bigl\|\nabla_{\boldsymbol{\theta}}^2 f_{\boldsymbol{\theta}^*}(\mathbf{x}_i)\bigr\|_2 .
\]
Тогда
\begin{equation}\label{ch1lc:eq:interp-hessian}
  \bigl\|
    \mathbf{H}_i(\boldsymbol{\theta}^*)
    - \mathbf{g}_i\mathbf{g}_i^\top
  \bigr\|_2
  \le \varepsilon_y M_{\nabla^2 f}
\end{equation}
для каждого $i$, и
\begin{equation}\label{ch1lc:eq:muf-interp-general}
  \mu_f(f\mid D)\le
  2\max_{1\le i\le m}\|\mathbf{g}_i\|_2^2
  + 2\varepsilon_y M_{\nabla^2 f} .
\end{equation}
Следовательно, из~\(\ref{ch1lc:eq:muf-interp-general}\) следует
\begin{equation}
  \bigl|\mathcal{L}_{k+1}(\boldsymbol{\theta}) - \mathcal{L}_k(\boldsymbol{\theta})\bigr|
  \le
  \frac{\varepsilon_y^2 + 2R^2\bigl(\max_{1\le i\le m}\|\mathbf{g}_i\|_2^2 + \varepsilon_y M_{\nabla^2 f}\bigr)}{k+1}
  + 2\,\varepsilon_g\,R .
\end{equation}
Если дополнительно $\|\mathbf{W}^{(p)\star}\|_2\le M_{\mathbf{W}}$ для каждого слоя и $\|\mathbf{x}_i\|_2\le M_{\mathbf{x}}$ для всех $i$, то
\begin{equation}
  \max_{1\le i\le m}\|\mathbf{g}_i\|_2^2
  \le L M_{\mathbf{x}}^2 M_{\mathbf{W}}^{2L-2},
\end{equation}
откуда
\begin{equation}
  \bigl|\mathcal{L}_{k+1}(\boldsymbol{\theta}) - \mathcal{L}_k(\boldsymbol{\theta})\bigr|
  \le
  \frac{\varepsilon_y^2 + 2R^2\bigl(L M_{\mathbf{x}}^2 M_{\mathbf{W}}^{2L-2} + \varepsilon_y M_{\nabla^2 f}\bigr)}{k+1}
  + 2\,\varepsilon_g\,R .
\end{equation}
В этой окрестности функция $f_{\boldsymbol{\theta}}$ дважды дифференцируема на наблюдаемых входах. Частный случай $\varepsilon_y=0$ соответствует точной интерполяции и сводит~\eqref{ch1lc:eq:interp-hessian} к тождеству Гаусса--Ньютона.
\end{theorem}
Теорема уточняет общую ландшафтную оценку: кроме члена $O(M_{\mathbf{H}}R^2/k)$ появляется вклад остатков и возможный стационарный член, связанный с неточным совпадением локальных минимумов на соседних объемах выборки.
Для приложений это означает, что даже при квадратичной потере одной только асимптотики $L(hM)^{2L}$ недостаточно, если остатки модели в опорной точке не малы.

\begin{corollary}[Линейная модель]\label{ch1lc:cor:linear}
Если $f_{\boldsymbol{\theta}}(\mathbf{x})=\mathbf{w}^{\top}\mathbf{x}$, где $\boldsymbol{\theta}=\mathbf{w}\in\mathbb{R}^{n}$, и используется та же квадратичная функция потерь~\eqref{ch1lc:eq:loss-i}, то
$\mathbf{H}_i(\boldsymbol{\theta})=\mathbf{x}_i\mathbf{x}_i^{\top}$ не зависит от $\mathbf{w}$ и $y_i$, поэтому
$\|\mathbf{H}_i\|_2=\|\mathbf{x}_i\|_2^2$.
При $\|\mathbf{x}_i\|_2\le M_{\mathbf{x}}$ полагаем $M_{\mathbf{H}}=M_{\mathbf{x}}^2$ в лемме~\ref{ch1lc:lem:hess-mse} и следствии~\ref{ch1lc:cor:muf}, заменив оценку для ReLU-сети этой ранговой матрицей Гессе.
Ландшафтная мера сложности~\eqref{ch1lc:eq:muf} в этом случае определяется разбросом ранговых матриц $\mathbf{x}_i\mathbf{x}_i^{\top}$ вокруг их выборочного среднего.
\end{corollary}

\begin{corollary}[Один скрытый слой, FC ReLU]\label{ch1lc:cor:shallow}
Рассмотрим один скрытый слой ширины~$h$: $\mathbb{R}^{n}\to\mathbb{R}^{h}\to\mathbb{R}$ с ReLU и матрицами весов без смещений при тех же спектральных ограничениях, что в лемме~\ref{ch1lc:lem:hess-mse}.
При $L=2$ оценка~\eqref{ch1lc:eq:muf-scale} дает наихудший случай
\[
    \mu_f(f\mid D)=O\!\bigl((hM)^{4}\bigr).
\]
Если дополнительно $|f_{\boldsymbol{\theta}^\star}(\mathbf{x}_i)-y_i|\le\varepsilon_y$ и $|w_{ij}|\le M$, то из~\eqref{ch1lc:eq:muf-interp-general} следует
\[
    \mu_f(f\mid D)
    \le
    2hM^{2}\max_{i}\bigl(\|\mathbf{x}_i\|_{1}^{2}+\|\mathbf{x}_i\|_{2}^{2}\bigr)
    +
    2\varepsilon_y M_{\nabla^2 f}.
\]
При $\|\mathbf{x}_i\|_{2}^{2}=\Theta(n)$, $\|\mathbf{x}_i\|_{1}=\Theta(n)$ это $O(hM^{2}n^{2}+\varepsilon_y M_{\nabla^2 f})$. Если $\varepsilon_y$ мало и $n\asymp h$, то ведущий член~$O(M^{2}h^{3})$.
\end{corollary}

Лемма~\ref{ch1lc:lem:hess-mse} также задает порядок ландшафтной меры сложности~\eqref{ch1lc:eq:muf} через глубину, ширину и ограничения на норму параметров.

\begin{corollary}[Масштабирование ландшафтной меры сложности]\label{ch1lc:cor:muf}
При условиях леммы~\ref{ch1lc:lem:hess-mse}, покоординатных ограничениях $|w_{ij}^{(p)}|\le M$ на соответствующих блоках весов размера $h\times h$ и фиксированном ограничении $M_{\mathbf{x}}$ на входы мера~\eqref{ch1lc:eq:muf} удовлетворяет оценке
\begin{equation}\label{ch1lc:eq:muf-scale}
  \mu_f(f\mid D) \;=\; O\!\bigl(L\,(hM)^{2L}\bigr),
\end{equation}
где $h$ обозначает ширину скрытого слоя, $L$~--- глубину, а $M$~--- покоординатное ограничение на веса. Ограничение $M_{\mathbf{x}}$ включено в неявную константу.
\end{corollary}

В \textbf{подразделе 2.3.2} приведены оценки сходимости ландшафта функции потерь для сверточных сетей.

\begin{theorem}[Сходимость ландшафта функции потерь для 1D-сверточных сетей]
Пусть параметры~$\boldsymbol{\theta}$ находятся в $R$-окрестности оптимума:
\[
    \|\boldsymbol{\theta} - \boldsymbol{\theta}^*\| \leqslant R,
\]
а функция потерь ограничена некоторой константой:
\[
    \exists  M_{\ell} > 0:  \forall i |\ell_i| \leqslant M_{\ell}.
\]
Пусть все объекты в наборе данных ограничены:
\[
    \exists M_{\mathbf{x}} > 0 \forall i  \|\mathbf{x}_i\| \leqslant M_{\mathbf{x}}.
\]
Тогда при тех же ограничениях на веса и входы:
\begin{align}
    & \big|\mathcal{L}_{k+1}(\boldsymbol{\theta}) - \mathcal{L}_{k}(\boldsymbol{\theta})\big| \leqslant \frac{2}{k+1}M_{\ell} + \frac{2}{k+1}R^2\sqrt{2}d^2M_{\mathbf{x}}^2(L+1)(C^2w^2k_{\mathrm{ker}}d)^L,
\end{align}
\end{theorem}

\begin{theorem}[Сходимость ландшафта функции потерь для 2D-сверточных сетей]
Пусть параметры $\boldsymbol{\theta}$ находятся в $R$-окрестности оптимума:
\[
    \|\boldsymbol{\theta} - \boldsymbol{\theta}^*\| \leqslant R.
\]
Функция потерь ограничена некоторой константой:
\[
    \exists  M_{\ell} > 0:  \forall i |\ell_i| \leqslant M_{\ell}.
\]
Пусть все объекты в наборе данных ограничены:
\[
    \exists M_{\mathbf{x}} > 0 \forall i  \|\mathbf{x}_i\| \leqslant M_{\mathbf{x}}.
\]

Тогда при тех же ограничениях на веса и входы справедливо:
\begin{align}
    & \big|\mathcal{L}_{k+1}(\boldsymbol{\theta}) - \mathcal{L}_{k}(\boldsymbol{\theta})\big| \leqslant \frac{2}{k+1}M_{\ell} + \frac{2}{k+1}R^2\sqrt{2}q^2M_{\mathbf{x}}^2(L+1)(C^2k_{\mathrm{ker}}^2w^2mn)^L,
\end{align}
где $q^2 = C^2k_{\mathrm{ker}}^2mn$.
\end{theorem}

\begin{corollary}[Асимптотика ландшафтной меры для 1D-сверточных сетей]
Ландшафтная мера сложности параметрической функции~$f$ 1D-сверточной модели глубокого обучения имеет асимптотику:
    \[
        \mu_f(f|D) \propto L(C^2M^2k_{\mathrm{ker}}d)^{L},
    \]
    где~$C$~--- максимальное число каналов,~$d$~--- длина входной последовательности,~$k_{\mathrm{ker}}$~--- размер свертки,~$M$~--- некоторая константа, ограничивающая параметры и данные.
\end{corollary}

\begin{corollary}[Асимптотика ландшафтной меры для 2D-сверточных сетей]
Ландшафтная мера сложности параметрической функции~$f$ 2D-сверточной модели глубокого обучения имеет асимптотику:
    \[
        \mu_f(f|D) \propto C^2k_{\mathrm{ker}}^2L(C^2k_{\mathrm{ker}}^2M^2mn)^{L},
    \]
    где~$C$~--- максимальное число каналов,~$m,n$~--- размеры входного изображения,~$k_{\mathrm{ker}}$~--- размер свертки,~$M$~--- некоторая константа, ограничивающая параметры и данные.
\end{corollary}
Оценки для сверточных сетей показывают, что число каналов, размер ядра и пространственный размер входа входят в верхние оценки мультипликативно.
Это позволяет сравнивать архитектурные решения не только по числу параметров, но и по их влиянию на локальную кривизну функции потерь.
Отсюда следует, что при выборе сверточной архитектуры сокращение числа каналов или размера ядра сглаживает ландшафт в большей степени, чем уменьшение числа слоев.

\begin{corollary}[Снижение ландшафтной меры при пулинге]
Пусть выполнены условия теоремы о сходимости ландшафта функции потерь для 2D-сверточных сетей, а сеть дополнительно содержит слой MaxPool2D или AvgPool2D с ядром~$k_{\mathrm{pool}}\times k_{\mathrm{pool}}$ в позиции~$l$.
Тогда верхняя оценка ландшафтной меры приобретает множитель $\bigl(1/k_{\mathrm{pool}}^2\bigr)^{L-l+2}$:
\[
    \mu_f(f|D) \propto C^2k_{\mathrm{ker}}^2L\Bigl(\frac{1}{k_{\mathrm{pool}}^2}\Bigr)^{L-l+2}(C^2k_{\mathrm{ker}}^2M^2mn)^{L}.
\]
В частности, более ранний пулинг, то есть меньшая~$l$, сильнее снижает верхнюю оценку сложности.
\end{corollary}
Пулинг частично компенсирует рост ландшафтной меры с глубиной и служит теоретически обоснованным фактором стабилизации.

В \textbf{подразделе 2.3.3} анализируется ландшафт функции потерь трансформерных сетей.
Выход одной головы самовнимания:
\[
    \mathrm{Attn}(\mathbf{X})
    =
    \mathrm{softmax}\!\Bigl(\frac{\mathbf{XW}_Q(\mathbf{XW}_K)^\top}{\sqrt{d_K}}\Bigr)\mathbf{XW}_V.
\]
Блок трансформера состоит из самовнимания, послойной нормализации и FFN. Локальная кривизна задается блоками~$\{\mathbf{W}_Q,\mathbf{W}_K,\mathbf{W}_V\}$ и полносвязной частью.

\begin{theorem}[Сходимость ландшафта функции потерь для трансформеров]
При $0\le \ell(\mathbf{f}_{\mathbf{w}^*}(\mathbf{x}_i),\mathbf{y}_i)\le B_{\ell}$ и ограниченных нормах матриц Гессе
\[
    \bigl|\mathcal{L}_{k+1}(\mathbf{w})-\mathcal{L}_k(\mathbf{w})\bigr|
    \le
    \frac{2B_{\ell}}{k+1}
    +
    \frac{M\|\mathbf{w}-\mathbf{w}^*\|_2^2}{k+1},
\]
где $M=\|\mathbf{H}\|_2$ для слоя самовнимания либо $M=M_{\mathrm{tr}}$ для полного блока.
\end{theorem}
Разложение источников:
\[
    \underbrace{\frac{2B_{\ell}}{k+1}}_{\text{ограниченность потерь}}
    +
    \underbrace{\frac{M\|\mathbf{w}-\mathbf{w}^*\|_2^2}{k+1}}_{\text{кривизна (самовнимание либо полный блок)}}.
\]
Константа~$M$ неоднородна по блокам трансформера, поэтому $\|\mathbf{H}\|_2$ для Q/K/V и FFN оценивается отдельно в гл.~3, а сложность трансформера не сводится к числу параметров или слоев.

В \textbf{разделе 2.4} на MNIST, FashionMNIST и CIFAR-10/100 проверены теоретические оценки: увеличение глубины замедляет сходимость ландшафта, увеличение ширины~--- ускоряет, в свою очередь для сверток исследованы гиперпараметры, а для трансформеров показана стабилизация ландшафта при росте объема данных.
Для полносвязных сетей варьировались число слоев и число нейронов в скрытых слоях.
С ростом глубины разность $\left|\mathcal{L}_{k+1}-\mathcal{L}_k\right|$ убывает медленнее, что согласуется с экспоненциальной зависимостью верхней оценки от $L$.
При фиксированной глубине увеличение ширины ускоряет эмпирическую стабилизацию, то есть модель получает большую выразительную способность без столь резкого роста множителя глубины.

\begin{figure}[h!]
\centering
\includegraphics[width=0.48\linewidth]{figures/chapter-1/landscape-hessian/figs/mnist_hidden_size.pdf}\hfill
\includegraphics[width=0.48\linewidth]{figures/chapter-1/landscape-hessian/figs/mnist_num_layers.pdf}
\caption{Зависимость $|\mathcal{L}_{k+1}(\boldsymbol{\theta})-\mathcal{L}_k(\boldsymbol{\theta})|$ от размера выборки~$k$ для полносвязной сети на MNIST: слева~--- при фиксированной глубине $L=5$ и вариации ширины $h$, справа~--- при фиксированной ширине $h=16$ и вариации глубины~$L$.}
\label{autoref:fig:fc-landscape-mnist}
\end{figure}

Для сверточных сетей анализировались число каналов, размер ядра, глубина и положение пулинга.
Зависимости подтверждают, что структурные элементы влияют на локальную меру сложности не только через число параметров, но и через распространение сигнала по слоям.
Отдельно рассмотрена регрессия для линейной и двухслойной ReLU-архитектур и сопоставление ландшафтной меры с эмпирической радемахеровской сложностью.
Радемахеровская сложность описывает равномерную емкость класса функций, а ландшафтная мера~--- разброс пообъектных матриц Гессе в опорной точке~$\boldsymbol{\theta}^*$. Величины дополняют, а не заменяют друг друга.

\begin{table}[h!]
\centering
\caption{Теоретические верхние порядки~$\mu_f$ и эмпирической радемахеровской сложности при квадратичной потере. Для двухслойной~$\mu_f$ спектральная оценка наихудшего случая сопоставлена с оценкой с учетом малости остатков~(следствие~\ref{ch1lc:cor:shallow}).}
\label{autoref:tab:mu-vs-rademacher}
\small
\begin{tabular}{@{}p{0.18\linewidth}p{0.38\linewidth}p{0.40\linewidth}@{}}
\toprule
 & Линейная модель & Двухслойная FC ReLU \\
\midrule
$\mu_f(f\mid D)$ &
$O(n)$&
$O((hM)^{4})$~(наихудший случай);
$O(hM^{2}n^{2}+\varepsilon_y M_{\nabla^2 f})$~(малые остатки);
при $n\asymp h$ и малом~$\varepsilon_y$~--- $O(M^{2}h^{3})$ \\
\addlinespace[0.35em]
$\hat{\mathfrak{R}}_m$ &
$O(W\sqrt{n/m})$ &
$O(\mathrm{poly}(h)/\sqrt{m})$ \\
\bottomrule
\end{tabular}
\end{table}

Для линейной модели~$\mathbf{H}_i=\mathbf{x}_i\mathbf{x}_i^{\top}$, поэтому $\mu_f=O(n)$ при $\|\mathbf{x}_i\|_{2}^{2}=\Theta(n)$, тогда как $\hat{\mathfrak{R}}_m=O(W\sqrt{n/m})$, где $W$~--- ограничение на норму вектора весов.
Для двухслойной сети оценка наихудшего случая~$O((hM)^{4})$ следует из общей спектральной оценки~$L=2$. При малых остатках квадратичная потеря подавляет член с~$\nabla^{2}f$, и рост по ширине смягчается до~$O(h^{3})$ при~$n=h$.
На рис.~\ref{autoref:fig:twolayer-h} эмпирическая~$\mu_f$ сопоставлена с опорными кривыми $\propto(Mh)^{4}$ и $\propto h^{3}$, а $\hat{\mathfrak{R}}_m$ показана на второй оси.

\begin{figure}[h!]
\centering
\includegraphics[width=0.78\linewidth]{figures/chapter-1/landscape-comparison/exp03_twolayer_sweep_h.png}
\caption{Двухслойная FC ReLU: эмпирическая~$\mu_f$~(левая ось, лог.) с опорными кривыми наихудшего случая $\propto(Mh)^{4}$ и GN-масштабирования при малых остатках $\propto h^{3}$, а эмпирическая~$\hat{\mathfrak{R}}_m$~(правая ось, лог.).}
\label{autoref:fig:twolayer-h}
\end{figure}

Для моделей семейства GPT-2 на корпусе WikiText-103 проанализирована динамика выборочной ландшафтной меры.
Сравнивались траектории функции потерь, валидационного качества и ландшафтной меры при разных настройках обучения.
Динамика ландшафтной меры несет информацию о геометрическом режиме обучения, не сводимую к одной только функции потерь: фаза уменьшения ошибки и фаза стабилизации геометрии не совпадают.
Дополнительно изучалась зависимость меры от времени обучения, валидационной ошибки и архитектуры.
У части моделей динамика немонотонна: убывание потерь сопровождается ростом или стабилизацией ландшафтной меры.
Локальная кривизна отражает не только качество аппроксимации, но и характер найденной области параметров.

Итог данной главы: общие меры $\mu_D$ и $\mu_f$ задают исходную формализацию, а основной вычислимый объект~--- ландшафтная мера параметрической модели.
Получены явные асимптотические оценки для основных архитектур и численное сопоставление с эмпирической радемахеровской сложностью. Это основа для оценок матриц Гессе и методов анализа достаточного объема выборки.
В частности, условие $k+1\ge(M_\ell+\mu_f(f|D)R^2)/\varepsilon$ показывает: при фиксированной точности $\varepsilon$ требуемый объем данных растет вместе с ландшафтной мерой~--- более «кривая» локальная геометрия требует большей выборки для той же стабилизации эмпирического риска.
Так вторая глава формализует связь сложности модели с достаточным объемом данных, а глава~4 дает наблюдаемые критерии оценки этого объема для практического применения без прямого вычисления матриц Гессе.

Ограничения постановки: детерминистический анализ, общее предположение об опорной точке минимума для последовательных размеров выборки и возможность улучшения верхних оценок за счет разреженности активаций.
Даже при этом ландшафтная мера дает вычислимо интерпретируемую связь архитектуры, локальной геометрии функции потерь и изменения эмпирического риска при росте выборки.

В \textbf{третьей главе} исследуются матрицы Гессе функции потерь для основных семейств нейросетевых архитектур.
Цель~--- сделать ландшафтную меру второй главы вычислимо оцениваемой.
Мера $\mu_f(f|D)$ задается разбросом пообъектных матриц Гессе, однако полное построение матрицы вторых производных для современных сетей вычислительно трудно.
В главе получены факторизованные представления матриц Гессе и верхние оценки их спектральной нормы через глубину, ширину, число каналов, размеры ядер и блоки самовнимания.

\begin{theorem}[Факторизация матрицы Гессе]
    Пусть функция нейронной сети $f_{\boldsymbol{\theta}}(\mathbf{x})$ представима в матричной форме через последовательность линейных операторов, нелинейных масок и параметрических преобразований. Тогда матрица Гессе функции потерь по параметрам допускает факторизацию
\[
    \mathbf{H}_O(\boldsymbol{\theta})
    =
    \mathbf{Q}^{\top}\mathbf{F}^{\top}\mathbf{A}\mathbf{F}\mathbf{Q},
\]
где $\mathbf{H}_O$~--- $G$-компонента матрицы Гессе, $\mathbf{A}$~--- вторые производные функции потерь по выходу, $\mathbf{F}$~--- распространение возмущений через архитектуру, $\mathbf{Q}$~--- параметризация слоев.
\end{theorem}
Факторизация разделяет три вклада: параметризацию слоев ($\mathbf{Q}$), распространение сигнала ($\mathbf{F}$) и вторые производные по выходу ($\mathbf{A}$).
Факторизация позволяет вывести порядки $L(hM)^{2L}$ для полносвязных сетей и аналогичные оценки для сверток и трансформеров.

Для полносвязных сетей получена оценка спектральной нормы с асимптотикой $\|\mathbf{H}_i(\boldsymbol{\theta})\|_2=O(L(hM)^{2L})$: экспоненциально по глубине и полиномиально по ширине.
Для одномерных и двумерных сверток оценки выражены через число каналов, размеры ядер и пространственные размеры входа.
Снижение ландшафтной меры при MaxPool и AvgPool оформлено следствием: множитель $(1/k_{\mathrm{pool}}^2)^{L-l+2}$ усиливается при более раннем пулинге.
Оценки для полносвязных и сверточных сетей, а также следствие о влиянии пулинга непосредственно следуют из факторизации матрицы Гессе.
Более подробные оценки приведены в полном тексте диссертации.

\begin{theorem}[Оценка нормы матрицы Гессе для слоя самовнимания]
Пусть $\|\cdot\|_2$~--- спектральная норма. Тогда для слоя самовнимания
\[
    \|\mathbf{H}_i(\mathbf{w}^*)\|_2 \leq M,
\]
где
\begin{align}
M &= 3\cdot\max \Bigg(\frac{2N}{d_V} \| \mathbf{X}\|^2_2, \\
    &\frac{8}{N^3 d_V d_K} \| \mathbf{W}_K\|_2^2 \| \mathbf{W}_V\|^2_2 \| \mathbf{X}\|^6_2 + \\
    &\quad+\frac{12}{d_V d_K} \sqrt{\min(N, d_V)} (N \|\mathbf{X}\|_2 \|\mathbf{W}_V \|_2 + \|\textbf{Target}\|_2) \| \mathbf{W}_V \|_2 \| \mathbf{W}_K\|^2_2 \| \mathbf{X}\|^5_2, \\
    &\frac{4}{N d_V \sqrt{d_K}} \| \mathbf{W}_V\|_2 \| \mathbf{W}_K \|_2 \| \mathbf{X}\|^4_2 +\\
    &\quad+\frac{4\sqrt{\min(N, d_V)}}{N^2\sqrt{d_K}} (N \|\mathbf{X}\|_2 \|\mathbf{W}_V \|_2 + \|\textbf{Target}\|_2) \|\mathbf{W}_K\|_2 \|\mathbf{X}\|^3_2,\\
    &\frac{8}{N^3 d_V d_K} \|\mathbf{W}_K\|_2 \|\mathbf{W}_Q\|_2 \| \mathbf{W}_V\|^2_2 \|\mathbf{X} \|^6_2 + \\
    &\quad+\frac{4\sqrt{\min(N, d_V)} (N \|\mathbf{X}\|_2 \|\mathbf{W}_V \|_2 + \|\textbf{Target}\|_2)}{N d_V \sqrt{d_K}} \|\mathbf{W}_V\|_2 \cdot\\
    &\quad\quad\cdot\Big(3N \|\mathbf{W}_K\|_2 \|\mathbf{W}_Q\|_2 \| \mathbf{X}\|^5_2 + \frac{d_V}{N} \|\mathbf{X}\|^3_2\Big)\Bigg).
\end{align}
\end{theorem}

Константа $M$ полиномиально зависит от норм входа и матриц $\mathbf{W}_Q,\mathbf{W}_K,\mathbf{W}_V$ и входит в оценку сходимости ландшафта трансформера как коэффициент при $\|\mathbf{w}-\mathbf{w}^*\|_2^2/(k+1)$.
Слагаемое $\frac{2N}{d_V}\|\mathbf{X}\|_2^2$ соответствует проекции значений, а остальные члены описывают взаимодействие запросов и ключей, нормированное функцией softmax.
Разные группы параметров трансформера по-разному влияют на локальную стабилизацию ландшафта, что согласуется с экспериментальной неоднородностью блоков запросов, ключей и значений.

Факторизация отделяет три источника сложности: параметризацию, прямое распространение сигнала и вторые производные функции потерь по выходам.
Это позволяет единообразно получать оценки для разных архитектур, не пересчитывая все вторые производные покомпонентно, а матрица Гессе выступает инструментом вывода асимптотик по глубине, ширине и структурным параметрам.

Для полносвязных сетей оценка спектральной нормы уточняет результат второй главы: рост сложности связан с произведением норм весовых матриц по слоям и накоплением вкладов второго порядка при обратном распространении.
При ограничении норм параметров значением $M$ и фиксированной ширине слоя $h$ верхняя оценка спектральной нормы принимает вид $L(hM)^{2L}$.
Отсюда следует, что глубина сильнее влияет на локальную геометрию, чем ширина.

Для сверток отдельно рассмотрены одномерный и двумерный случаи.
В одномерном оценка зависит от числа каналов $C$, размера ядра $k_{\mathrm{ker}}$, длины входа $d$ и глубины $L$, в двумерном~--- еще от размеров изображения $m$ и $n$.
Пулинг влияет на ландшафтную меру через множитель $(1/k_{\mathrm{pool}}^2)^{L-l+2}$, что объясняет стабилизирующую роль раннего пулинга и согласуется с экспериментами второй главы.

Для трансформеров дополнительно получены блочные оценки нормы полной матрицы Гессе блока и вспомогательные соотношения для послойной нормализации.
Вклады блоков самовнимания в кривизну неодинаковы: в эксперименте на небольшой модели на MNIST наибольшие спектральные нормы у блоков Key и Value.
Для регуляризации и оптимизации это аргумент в пользу раздельного анализа групп параметров.

\begin{figure}[h!]
\centering
\includegraphics[width=0.46\linewidth]{figures/chapter-2/transformer/parameters_norm.pdf}\hfill
\includegraphics[width=0.46\linewidth]{figures/chapter-2/transformer/hessians_norm.pdf}
\caption{Спектральные нормы блоков параметров трансформера (слева) и соответствующих блоков матрицы Гессе (справа) на батче MNIST. Наибольшие значения норм соответствуют блокам Key и Value.}
\label{autoref:fig:transformer-hessian-norms}
\end{figure}

В главе также обсуждается сложность полного вычисления матриц Гессе.
Размер матрицы квадратичен по числу параметров, поэтому прямое вычисление для современных сетей требует очень больших затрат памяти и времени.
Факторизованные выражения и спектральные оценки позволяют вместо полной матрицы анализировать ее спектральную норму.
Это необходимо для вычисления ландшафтной меры, которая задается разбросом пообъектных матриц Гессе.

Вычислительные эксперименты проверяют оценки эмпирически.
Для полносвязных сетей подтверждается согласованность порядка $L(hM)^{2L}$ с ростом спектральных характеристик при увеличении глубины и ширины, для сверток~--- варьирование каналов, ядра и положения пулинга подтверждает редуцирующее влияние раннего пулинга.
Эксперимент на трансформере, представленный на рис.~\ref{autoref:fig:transformer-hessian-norms}, проведен на небольшой архитектуре и не охватывает большие языковые модели.
Тем не менее он демонстрирует неоднородность вкладов блоков параметров, наибольший вклад в локальную кривизну вносят блоки Key и Value.

В \textbf{четвертой главе} достаточный размер~$m^*$ оценивается по наблюдаемой устойчивости обучения без построения явного спектра~$\mathbf{H}$. Во второй главе получена теоретическая оценка требуемого размера выборки: при~$\mu_D(D)=C|D|$ и требовании $|\mathcal{L}_{k+1}-\mathcal{L}_k|\le\varepsilon$ в $R$-окрестности опорной точки
\[
    k+1 \ge \frac{M_\ell+\mu_f(f|D)R^2}{\varepsilon}.
\]
Достаточный размер~$m^*$ определяется по устойчивости правдоподобия и близости апостериорных распределений~$p_k$ и~$p_{k+1}$ на соседних подвыборках.

\paragraph{Достаточность по устойчивости правдоподобия.}
Пусть~$\mathfrak{D}_m$ уже не меньше искомого~$m^*$, $\hat{\mathbf{w}}_k$~--- оценка параметров на подвыборке размера~$k$, $L(\mathfrak{D}_m,\hat{\mathbf{w}}_k)$~--- правдоподобие на полной выборке.
На практике математическое ожидание $\mathbb{E}$ и дисперсия $\mathbb{D}$ вычисляются по $B$ бутстрэп-подвыборкам размера $k$.

\begin{definition}[D- / M-достаточность]
При~$\varepsilon>0$ размер~$m^*$ называется $D$-достаточным, если для всех~$k\ge m^*$
\[
    D(k)=\mathbb{D}_{\hat{\mathbf{w}}_k} L(\mathfrak{D}_m,\hat{\mathbf{w}}_k)\le\varepsilon,
\]
и $M$-достаточным, если для всех~$k\ge m^*$
\[
    M(k)=\bigl|
        \mathbb{E}_{\hat{\mathbf{w}}_{k+1}} L(\mathfrak{D}_m,\hat{\mathbf{w}}_{k+1})
        -
        \mathbb{E}_{\hat{\mathbf{w}}_{k}} L(\mathfrak{D}_m,\hat{\mathbf{w}}_{k})
    \bigr|\le\varepsilon.
\]
\end{definition}
$D(k)$~--- устойчивость значения правдоподобия, $M(k)$~--- близость к стабилизации $|\mathcal{L}_{k+1}-\mathcal{L}_k|$.

\paragraph{Достаточность по близости апостериорных распределений.}
Подвыборки близки, если $|\mathcal{I}_1\triangle\mathcal{I}_2|=1$. Пусть~$p_k$~--- апостериор на размере~$k$.
\[
    \mathrm{s\textrm{-}score}(g_1,g_2)
    =
    \frac{\int g_1(\mathbf{w})g_2(\mathbf{w})\,d\mathbf{w}}
         {\max_{\mathbf{b}}\int g_1(\mathbf{w}-\mathbf{b})g_2(\mathbf{w})\,d\mathbf{w}}.
\]

\begin{definition}[KL- / S-достаточность]
\begin{align}
    KL(k)&=D_{\mathrm{KL}}(p_k\|p_{k+1})
    =\int p_k\log\frac{p_k}{p_{k+1}}\,d\mathbf{w}
    \le\varepsilon,\\
    S(k)&=\mathrm{s\textrm{-}score}(p_k,p_{k+1})\ge 1-\varepsilon.
\end{align}
\end{definition}

\begin{theorem}[Сходимость апостериоров в линейной регрессии]
При $\|\mathbf{x}\|_2\le M$, $|y|\le M$ и $\lambda_{\min}(\mathbf{X}_k^\top\mathbf{X}_k)=\omega(\sqrt{k})$ для нормального априора
\[
    \|\mathbf{m}_{k+1}-\mathbf{m}_k\|_2\to 0,\qquad
    \|\boldsymbol{\Sigma}_{k+1}-\boldsymbol{\Sigma}_k\|_F\to 0.
\]
\end{theorem}

Эмпирически на синтетике и наборах Liver Disorders, Boston Housing, Diabetes, Forest Fires, Servo:
\[
    D(k),M(k),KL(k)\to 0,\qquad S(k)\to 1\quad(k\uparrow).
\]

\begin{figure}[h!]
\centering
\includegraphics[width=0.92\linewidth]{figures/chapter-3/posterior-distribution/synthetic-regression.pdf}
\caption{Сходимость $KL(k)\to 0$ и $S(k)\to 1$ на синтетической регрессии.}
\label{autoref:fig:kl-s-convergence}
\end{figure}
Критерии не заменяют ландшафтную меру $\mu_f$, а позволяют находить $m^*$ по наблюдаемым данным, когда матрицы Гессе недоступны.

В \textbf{пятой главе} рассматриваются методы снижения сложности параметрических моделей.
Они уменьшают число обучаемых параметров либо перераспределяют информацию между моделями, сохраняя качество аппроксимации.
К защищаемым методам относятся прореживание по ковариации градиентов, многодоменная дистилляция и антидистилляция, а байесовская дистилляция выступает вспомогательным теоретическим инструментом согласования архитектур.

\paragraph{Прореживание по ковариации градиентов.}
Эмпирический риск $\mathcal{L}(\mathbf{w},\mathfrak{D})=\frac1m\sum_i \ell(\mathbf{x}_i,y_i,\mathbf{w})$.
На итерации оптимизации обновляется оценка ковариации градиентов:
\[
    \mathbf{C}_t
    =
    (1-\kappa_t)\mathbf{C}_{t-1}
    +
    \kappa_t\bigl(\mathbf{g}_{1,t}-\mathbf{g}_{S,t}\bigr)\bigl(\mathbf{g}_{1,t}-\mathbf{g}_{S,t}\bigr)^\top,
    \qquad
    \kappa_t=\tfrac1t,
\]
где $\mathbf{g}_{S,t}$~--- градиент на подвыборке размера~$S$, $\mathbf{g}_{1,t}$~--- на одном ее элементе.
По диагонали~$\mathbf{C}_{t_0}$ строится порядок индексов $\mathcal{I}_{\mathbf{w}_{t_0}}$, а фиксация наименее значимых параметров~--- маской~$\boldsymbol{\alpha}(\zeta)$:
\[
    \mathbf{w}_t
    =
    \mathbf{w}_{t-1}
    +
    \boldsymbol{\alpha}(\zeta)\cdot\Delta\mathbf{w}(\mathbf{g}_{S,t},\mathbf{w}_{t-1},\ldots).
\]

Метод Белсли: пусть $\mathbf{A}_{\mathrm{ps}}$~--- ковариация параметров в оптимуме,
\[
    \mathbf{A}_{\mathrm{ps}}=\mathbf{U}\boldsymbol{\Lambda}\mathbf{V}^\top,\qquad
    \eta_j=\frac{\max_i\Lambda_{ii}}{\Lambda_{jj}},\qquad
    \xi=\arg\max_j\eta_j;
\]
по дисперсионным долям~$q_j$ удаляются мультиколлинеарные направления.
Эксперименты~(Wine и др.): удаление до~$\approx 80\%$ параметров без существенной потери качества. Метод Белсли устойчивее к шуму.

\begin{figure}[h!]
\centering
\includegraphics[width=0.78\linewidth]{figures/chapter-4/belsli/Wine/All.pdf}
\caption{Точность на Wine от доли удаленных параметров: оптимальное прореживание, вариационный метод и метод Белсли допускают удаление~$\approx 80\%$.}
\label{autoref:fig:belsley-wine}
\end{figure}

\paragraph{Байесовская дистилляция.}
Априорное распределение параметров ученика задается апостериорным распределением параметров учителя $p(\mathbf{u}|\mathfrak{D})=\mathcal{N}(\mathbf{m},\boldsymbol{\Sigma})$.
Маргинальное правдоподобие
\[
    \mathcal{M}(\mathfrak{D},\mathbf{A})
    =
    \int p(\mathfrak{D}|\mathbf{w})\,p(\mathbf{w}|\mathbf{A})\,d\mathbf{w}.
\]
Прямое вычисление интеграла затруднительно, поэтому используется вариационное распределение~$q(\mathbf{w})$, оптимизируемое минимизацией~$D_{\mathrm{KL}}(q\|p(\mathbf{w}|\mathbf{A}))$ с учетом ожидания логарифма правдоподобия по~$q$.
Ниже~--- нормальность апостериорного распределения при удалении нейрона/слоя.
\begin{theorem}[Распределение параметров при удалении нейрона]
Пусть выполняются условия:
\begin{enumerate}
    \item апостериорное распределение параметров модели учителя имеет вид
    \[
        p(\mathbf{u}|\mathfrak{D})=\mathcal{N}(\mathbf{m},\boldsymbol{\Sigma});
    \]
    \item число слоев модели учителя равно числу слоев модели ученика: $T=L$;
    \item размеры соответствующих слоев удовлетворяют условию $n_t^{(\mathrm{tr})}\ge n_t^{(\mathrm{st})}$.
\end{enumerate}
Пусть преобразование $\boldsymbol{\phi}(t,\mathbf{u})$ удаляет один нейрон из $t$-го слоя модели учителя, а $\boldsymbol{\upsilon}=\boldsymbol{\phi}(t,\mathbf{u})$~--- вектор оставшихся параметров. Тогда распределение параметров после такого преобразования также является нормальным:
\[
    p(\boldsymbol{\upsilon}|\mathfrak{D})
    =
    \mathcal{N}(\boldsymbol{\mu}_{\boldsymbol{\upsilon}},
    \boldsymbol{\Xi}_{\boldsymbol{\upsilon},\boldsymbol{\upsilon}}).
\]
При этом параметры условного нормального распределения вычисляются как
\[
\begin{aligned}
    \boldsymbol{\mu}
    &=
    \mathbf{m}_{\bar{\boldsymbol{\nu}}_1}
    +
    \boldsymbol{\Sigma}_{\bar{\boldsymbol{\nu}}_1,\boldsymbol{\nu}_1}
    \boldsymbol{\Sigma}_{\boldsymbol{\nu}_1,\boldsymbol{\nu}_1}^{-1}
    (\mathbf{0}-\mathbf{m}_{\boldsymbol{\nu}_1}),\\
    \boldsymbol{\Xi}
    &=
    \boldsymbol{\Sigma}_{\bar{\boldsymbol{\nu}}_1,\bar{\boldsymbol{\nu}}_1}
    -
    \boldsymbol{\Sigma}_{\bar{\boldsymbol{\nu}}_1,\boldsymbol{\nu}_1}
    \boldsymbol{\Sigma}_{\boldsymbol{\nu}_1,\boldsymbol{\nu}_1}^{-1}
    \boldsymbol{\Sigma}_{\boldsymbol{\nu}_1,\bar{\boldsymbol{\nu}}_1},
\end{aligned}
\]
где $\boldsymbol{\nu}_1$~--- зануляемые параметры, $\boldsymbol{\nu}_2$~--- удаляемые параметры, $\bar{\boldsymbol{\nu}}_1=[\boldsymbol{\upsilon}^{\mathsf{T}},\boldsymbol{\nu}_2^{\mathsf{T}}]^{\mathsf{T}}$, а $\boldsymbol{\mu}_{\boldsymbol{\upsilon}}$ и $\boldsymbol{\Xi}_{\boldsymbol{\upsilon},\boldsymbol{\upsilon}}$ являются соответствующими подвектором и подматрицей.
\end{theorem}

\paragraph{Распределение параметров при удалении слоя.}
\begin{theorem}[Распределение параметров при удалении слоя]
Пусть выполняются условия:
\begin{enumerate}
    \item апостериорное распределение параметров модели учителя имеет вид
    \[
        p(\mathbf{u}|\mathfrak{D})=\mathcal{N}(\mathbf{m},\boldsymbol{\Sigma});
    \]
    \item размеры соседних слоев модели учителя совпадают, $n_t^{(\mathrm{tr})}=n_{t-1}^{(\mathrm{tr})}$, то есть матрица $\mathbf{U}_t$ является квадратной;
    \item функция активации удовлетворяет свойству идемпотентности $\boldsymbol{\sigma}\circ\boldsymbol{\sigma}=\boldsymbol{\sigma}$.
\end{enumerate}
Пусть преобразование $\boldsymbol{\psi}(t,\mathbf{u})$ удаляет $t$-й слой модели учителя, а $\boldsymbol{\upsilon}=\boldsymbol{\psi}(t,\mathbf{u})$~--- вектор оставшихся параметров.
Тогда апостериорное распределение оставшихся параметров также является нормальным:
\[
\begin{aligned}
    p(\boldsymbol{\upsilon}|\mathfrak{D})
    =
    \mathcal{N}\bigl(
    &\mathbf{m}_{\boldsymbol{\upsilon}}
    +
    \boldsymbol{\Sigma}_{\boldsymbol{\upsilon},\bar{\boldsymbol{\upsilon}}}
    \boldsymbol{\Sigma}_{\bar{\boldsymbol{\upsilon}},\bar{\boldsymbol{\upsilon}}}^{-1}
    (\mathbf{i}-\mathbf{m}_{\bar{\boldsymbol{\upsilon}}}),\\
    &\boldsymbol{\Sigma}_{\boldsymbol{\upsilon},\boldsymbol{\upsilon}}
    -
    \boldsymbol{\Sigma}_{\boldsymbol{\upsilon},\bar{\boldsymbol{\upsilon}}}
    \boldsymbol{\Sigma}_{\bar{\boldsymbol{\upsilon}},\bar{\boldsymbol{\upsilon}}}^{-1}
    \boldsymbol{\Sigma}_{\bar{\boldsymbol{\upsilon}},\boldsymbol{\upsilon}}
    \bigr),
\end{aligned}
\]
где $\bar{\boldsymbol{\upsilon}}$~--- вектор параметров удаляемого слоя, а
\[
    \mathbf{i}=
    [\underbrace{1,0,\ldots,0}_{n_t},
    \underbrace{0,1,\ldots,0}_{n_t},
    \ldots,
    \underbrace{0,\ldots,1}_{n_t}]^{\mathsf{T}}.
\]
\end{theorem}

\paragraph{Многодоменная дистилляция.}
Учитель~$\mathbf{f}:\mathbb{X}_{\mathrm{s}}\to\mathbb{Y}'$ на домене~$A$, ученик~$\mathbf{g}:\mathbb{X}_{\mathrm{t}}\to\mathbb{Y}'$ на домене~$B$, а связь~$\varphi:\mathbb{X}_{\mathrm{t}}\to\mathbb{X}_{\mathrm{s}}$ инъективна, то есть агрегаты соответствуют близким генеральным совокупностям.
\[
\begin{aligned}
    \mathcal{L}(\mathbf{w},\mathbf{X},\mathbf{Y},\mathbf{f},\varphi)
    &=
    -\lambda\sum_{i,r}\mathbb{I}[y_i=r]\log g^r(\mathbf{x}_i,\mathbf{w})\\
    &\quad
    -(1-\lambda)\sum_{i,r}(f\circ\varphi)^r(\mathbf{x}_i)\log g^r(\mathbf{x}_i,\mathbf{w}),
\end{aligned}
\]
\[
\hat{\mathbf{w}}=\arg\min_{\mathbf{w}}\mathcal{L}.
\]
На ImageNet вариант с~$\varphi$ лучше базового без дистилляции, а на OPUS-100~(fr-en, de-en) снижается CE и растет BLEU относительно ученика без учителя.

\paragraph{Антидистилляция.}
Простая модель~$\mathbf{g}_{\mathrm{tr}}(\cdot,\hat{\mathbf{u}})$ на~$\mathfrak{D}_1$ инициализирует более сложную~$\mathbf{f}_{\mathrm{st}}(\cdot,\mathbf{w})$:
\begin{equation}
    \mathcal{L}(\mathbf{w})
    =
    \lambda_1\mathcal{L}_{\mathrm{ce}}(\mathbf{w},\mathfrak{D}_1)
    +
    \lambda_2\|\mathbf{u}-\mathrm{Pr}[\mathbf{w}]\|_2^2
    +
    \lambda_3\sum_{(\mathbf{x},y)\in\mathfrak{D}_1}
        \mathbb{E}_{\mathbf{x}'\sim U_\delta(\mathbf{x})}\mathcal{L}_{\mathrm{ce}}(\mathbf{w},\mathbf{x}',y)
    +
    \lambda_4\,\mathrm{tr}\Bigl(\frac{\partial^2\mathcal{L}_{\mathrm{ce}}}{\partial\mathbf{w}^2}\Bigr),
\end{equation}
где $\mathcal{L}_2$~--- близость параметров, $\mathcal{L}_3$~--- устойчивость к шуму входа, $\mathcal{L}_4$~--- регуляризация кривизны~($\lambda_2>0$).
На Fashion-MNIST: точность $0{,}86\pm 0{,}05$, FGSM $0{,}57\pm 0{,}03$, шум параметров $0{,}73\pm 0{,}04$.
Снижение размерности пространства обучаемых параметров уменьшает число параметров модели при незначительном изменении качества аппроксимации.
Этот результат выносится на защиту в качестве пятого положения.

В \textbf{шестой главе} теоретический аппарат применяется к прикладным задачам, в частности к многозадачному обучению с LoRA-адаптерами, декодированию фМРТ и оценке качества данных.

Для адаптации предобученного классификатора рассматривается низкоранговое обновление
\[
    \Delta W=AB,\qquad A\in\mathbb{R}^{d\times r},\ B\in\mathbb{R}^{r\times k},\ r\ll\min(d,k),
\]
число обучаемых параметров~$r(d+k)$ вместо~$dk$.
Ожидаемый и эмпирический риски при замороженном~$\Phi_0$:
\[
    L(\Theta)=\mathbb{E}\bigl[-\log P_{\Phi_0+\Theta}(c\mid X)\bigr],
    \qquad
    \mathscr{L}_n(\Theta)=-\frac1n\sum_{i=1}^n\log P_{\Phi_0+\Theta}(c_i\mid X_i).
\]
Выход классификатора сохраняет вид
\[
    \hat{\mathbf{y}}=\mathrm{softmax}\bigl((W+\Delta W)^\top\mathbf{x}\bigr).
\]
Для многозадачного обучения при масштабировании $\|w_{\mathrm{shared}}\|\le B_{\mathrm{enc}}/\sqrt{T}$ верхняя оценка эмпирической радемахеровской сложности на задачу уменьшается не менее чем в~$1/\sqrt{T}$ раз, что устанавливается теоремой о радемахеровской сложности на задачу при многозадачном обучении.

\begin{theorem}[Состоятельность оценки]
Пусть $\mathcal{D} = \{(X_i, c_i)\}_{i=1}^n$~--- независимые одинаково распределенные пары из распределения $P_{\mathrm{true}}$ на $\mathcal{X}\times [N_c]$, где $[N_c]=\{1,\ldots,N_c\}$.
Рассмотрим компактное множество $K\subset\mathbb{R}^p$ и параметрическую модель $P_{\Phi_0+\Theta}(c\mid X)$ при $\Theta\in K$, где $\Phi_0$~--- замороженный вектор предобученных параметров.
Предположим:
\begin{enumerate}
    \item Существует единственная точка $\Theta^\star \in K$, минимизирующая ожидаемый риск
    \[
        L(\Theta) = \mathbb{E}_{(X,c)\sim P_{\mathrm{true}}}\bigl[-\log P_{\Phi_0+\Theta}(c\mid X)\bigr],\qquad \Theta\in K.
    \]
    \item Функция $(X,c,\Theta)\mapsto -\log P_{\Phi_0+\Theta}(c\mid X)$ непрерывна на своей области допустимых значений и
    \[
        \mathbb{E}_{(X,c)\sim P_{\mathrm{true}}}\Bigl[\,\sup_{\Theta\in K}\bigl|\log P_{\Phi_0+\Theta}(c\mid X)\bigr|\,\Bigr] < \infty.
    \]
    \item Эмпирический риск задается в виде
    \begin{equation}
        \mathscr{L}_n(\Theta) = -\frac{1}{n} \sum_{i=1}^n \log P_{\Phi_0+\Theta}(c_i\mid X_i),
    \end{equation}
    а оценка $\widehat{\Theta}_n$ выбирается как измеримый минимизатор $\mathscr{L}_n$ на $K$; если минимум не достигается, используется $\varepsilon_n$-минимизатор с $\varepsilon_n\downarrow 0$.
\end{enumerate}
Тогда $\widehat{\Theta}_n \xrightarrow{P} \Theta^\star$ при $n\to\infty$.
\end{theorem}

\begin{theorem}[Корректность при низкоранговых обновлениях]
Предположим следующее:
\begin{enumerate}
    \item Выходной слой задается в виде
    \begin{equation}
        \hat{\mathbf{y}} = \operatorname{softmax}\!\left(W_{\mathrm{upd}}^{\top} \mathbf{x}\right),
    \end{equation}
    где $\mathbf{x} \in \mathbb{R}^d$~--- это вектор признаков BERT, $W \in \mathbb{R}^{d \times k}$~--- замороженные предобученные веса, а
    \begin{equation}
        W_{\mathrm{upd}} = W + \Delta W.
    \end{equation}

    \item Вместо непосредственного обучения $\Delta W \in \mathbb{R}^{d \times k}$, обновление параметризуется в низкоранговой форме:
    \begin{equation}
        \Delta W = A B,
        \quad A \in \mathbb{R}^{d \times r},
        B \in \mathbb{R}^{r \times k},
        r \ll \min(d,k).
    \end{equation}

    \item Выполнены условия теоремы о состоятельности оценки, так что модель остается статистически состоятельной при минимизации эмпирического риска.
\end{enumerate}

Тогда при параметризации выход модели $\hat{\mathbf{y}}$ сохраняется в том смысле, что низкоранговое обновление не искажает корректность классификационного слоя.
\end{theorem}

\begin{corollary}[LoRA с теоретическим обоснованием]
Объединяя теорему о состоятельности оценки и теорему о корректности при низкоранговых обновлениях, получаем, что тонкая настройка на основе низкоранговой адаптации одновременно сохраняет статистическую состоятельность оценки и корректность выходного слоя, при этом требуя существенно меньшего числа обучаемых параметров.
\end{corollary}

\begin{theorem}[Радемахеровская сложность на задачу при MTL]
Пусть $S_t=\{x_i\}_{i=1}^n$~--- выборка фиксированной целевой задачи $t$ с $\|x_i\|_2 \le R$.
Пусть $\phi(\cdot;w)$~--- энкодер, и рассмотрим линейные головы
$f_{w,h}(x)=h^\top \phi(x;w)$ с $\|h\|_2 \le B_{\mathrm{head}}$.
Предположим:
\begin{enumerate}
\item Ограничение на признаки: для всех $x,w$
$\|\phi(x;w)\|_2 \le B_{\mathrm{Lip}}\,\|w\|_2\,\|x\|_2$.
\item Энкодер однозадачного обучения удовлетворяет $\|w_{\mathrm{enc}}\|\le B_{\mathrm{enc}}$,
общий энкодер многозадачного обучения удовлетворяет $\|w_{\mathrm{shared}}\|\le B_{\mathrm{shared}}$.
\item Многозадачное масштабирование: $B_{\mathrm{shared}} \le B_{\mathrm{enc}}/\sqrt{T}$.
\end{enumerate}

Обозначим через $\widehat{\mathfrak R}_n(\cdot;S_t)$ эмпирическую радемахеровскую сложность на $S_t$.
Пусть $\mathcal{F}_{\mathrm{STL}}^{(t)}$ совпадает с семейством $\mathcal{F}(B_{\mathrm{enc}})$, а $\mathcal{F}_{\mathrm{MTL}}^{(t)}$~--- с семейством $\mathcal{F}(B_{\mathrm{shared}})$, где класс $\mathcal{F}(B)$ состоит из линейных голов фиксированной нормы на энкодере.
Введем функцию
\begin{equation}
U_n(B) := \frac{B_{\mathrm{head}}\,B_{\mathrm{Lip}}\,R\,B}{\sqrt{n}}.
\end{equation}
Тогда
\begin{equation}
\widehat{\mathfrak R}_n\!\big(\mathcal{F}_{\mathrm{MTL}}^{(t)};S_t\big)\le U_n(B_{\mathrm{shared}})\le \frac{1}{\sqrt{T}}\,U_n(B_{\mathrm{enc}}),
\qquad
\widehat{\mathfrak R}_n\!\big(\mathcal{F}_{\mathrm{STL}}^{(t)};S_t\big)\le U_n(B_{\mathrm{enc}}).
\end{equation}
Следовательно, одна и та же величина вида $U_n(B)$ дает верхнюю оценку эмпирической радемахеровской сложности в STL- и MTL-постановках, а предположение~3 обеспечивает не менее чем $1/\sqrt{T}$-кратное уменьшение шаблонной верхней оценки при переходе от $B_{\mathrm{enc}}$ к $B_{\mathrm{shared}}$.
\end{theorem}

Таким образом, при справедливом масштабировании нормы энкодера многозадачное обучение снижает верхнюю оценку сложности на задачу в $\sqrt{T}$ раз без изменения числа параметров. Это теоретически объясняет улучшение обобщения в многозадачной постановке и ниже подтверждается экспериментально на детекции машинно-генерированного текста.

\paragraph{Многозадачное обучение и адаптеры LoRA.}
В экспериментах по детекции машинно-генерированного текста LoRA с рангом $r=8$ требует только $0{,}16\%$ параметров полной модели.
Снижение метрик качества относительно полной тонкой настройки невелико, потери на валидации уменьшаются на $36{,}3\%$ при ускорении обучения на $12{,}6\%$.

\begin{figure}[h!]
\centering
\includegraphics[width=0.72\linewidth]{figures/chapter-5/rademacher/lora_rank_vs_f1-1.pdf}
\caption{Зависимость $F_1$ от ранга~$r$ адаптеров LoRA (DeBERTa-v3-base) в задаче детекции машинно-генерированного текста. Наилучшее качество достигается при $r=8$.}
\label{autoref:fig:lora-rank}
\end{figure}

Многозадачная постановка с тремя выходными головками повышает F1 с $0{,}781$ до $0{,}826$ и ROC-AUC с $0{,}788$ до $0{,}834$.
Оценка эмпирической радемахеровской сложности уменьшается с $0{,}0159 \pm 0{,}0009$ до $0{,}0111 \pm 0{,}0010$, что качественно согласуется с уменьшением измеряемой сложности при многозадачной регуляризации энкодера.
Таким образом, детекция машинно-генерированного текста связана с общей темой диссертации: LoRA уменьшает число обучаемых параметров, многозадачное обучение снижает оценку сложности на задачу, а метрики показывают, что снижение сложности не приводит к существенной потере качества.
Адаптерная тонкая настройка~--- пример практического снижения сложности модели.

\paragraph{Декодирование фМРТ.}
В декодировании фМРТ предложена реконструкция томографических данных из видеопоследовательностей с предварительным сжатием фМРТ-представлений.
Снижение размерности существенно сокращает время обучения при сопоставимом качестве реконструкции: используются представления кадров ResNet152 и линейная регрессия с $L_2$-регуляризацией для предсказания изменений сигнала.
При коэффициентах сжатия признакового описания 2, 4 и 8 время обучения падает с $36{,}3$~с до $6{,}7$, $1{,}6$ и $1{,}4$~с.
Оптимальное $\alpha\approx1000$ сохраняется при разных коэффициентах сжатия, поэтому процедура снижения размерности устойчива.

\paragraph{Оценка качества данных.}
В детекции машинно-генерированного контента оценивается не только детектор, но и качество оценочных наборов.
$\text{KL}_{\text{TTS}}$ сравнивает распределения признаков текстов, PHD~--- топологию облака представлений, $\Delta_{\text{shift}}$~--- устойчивость к адверсарным сдвигам, $\text{KL}_{\text{shuffle}}$~--- чувствительность к перемешиванию предложений.
Высокие показатели на одном наборе не гарантируют устойчивости на другом.
Качественные наборы характеризуются близкими топологическими распределениями человеческих и машинных текстов и лучше подходят для оценки современных детекторов.
Сложность данных нельзя сводить к объему выборки: два набора одного размера различаются по устойчивости к возмущениям и по перекрытию распределений «человек/машина».
Контроль качества данных~--- необходимое дополнение к анализу сложности модели при построении и сравнении детекторов.

Последняя глава показывает: аппарат сложности полезен не только для анализа архитектур, но и для проектирования адаптеров, снижения размерности биомедицинских данных и диагностики качества обучающих наборов.
Этим завершается линия диссертации от теоретических мер сложности к практическим методам анализа моделей и данных.

\section*{Основные результаты и выводы}
В диссертации разработан математический аппарат оценки согласованности и снижения сложности моделей глубокого обучения и данных на основе теории мер, анализа матриц Гессе и ландшафта оптимизационной задачи.
Основные результаты:
\begin{enumerate}
    \item Формальный аппарат мер сложности выборки $\mu_D$ и модели $\mu_f$ и критерий согласованности $\mu_f(f)\le\mu_D(D)$.
    \item Ландшафтная мера $\mu_f(f|D)$ через разброс пообъектных матриц Гессе и ее связь с приращением эмпирического риска при росте выборки.
    \item Теоретические оценки ландшафтной меры и спектральных норм матриц Гессе для полносвязных, сверточных и трансформерных архитектур: учитываются роль глубины, ширины, каналов, ядер и блоков самовнимания.
    \item Предложены критерии достаточного объема выборки ($D$-, $M$-, $KL$- и $S$-достаточность) на основе устойчивости значения правдоподобия и близости апостериорных распределений. Для линейной регрессии доказана сходимость моментов апостериорных распределений, а связь критериев со сложностью выражается через условную сложность выборки и стабилизацию эмпирического риска; критерии дают наблюдаемую оценку $m^*$ без прямого вычисления матриц Гессе.
    \item Методы снижения и перераспределения сложности: прореживание по ковариации градиентов и метод Белсли, байесовская и многодоменная дистилляция, антидистилляция.
    \item Для LoRA~--- состоятельность оценки при стандартных условиях, а для MTL~--- сравнение верхних оценок эмпирической радемахеровской сложности на задачу при справедливом масштабировании нормы энкодера.
    \item Прикладные эксперименты по детекции машинно-генерированного текста, декодированию фМРТ и оценке качества наборов данных подтверждают применимость аппарата.
\end{enumerate}
Совокупность результатов формализует оценку согласованности и снижение сложности моделей и данных в рамках единого подхода, применимого к широкому классу нейросетевых архитектур.

\renewcommand{\bibname}{Список работ, опубликованных автором по теме диссертации}
\begin{thebibliography}{99}
\bibitem{grabovoy2026landscape}
	Grabovoy~A.\;V. Landscape Measure of Deep Learning Models // Pattern Recognition and Image Analysis: Advances in Mathematical Theory and Applications. 2026. Vol.~36, no.~2. pp.~708--716. Pleiades Publishing, Ltd (Road Town, United Kingdom).

\bibitem{grabovoy2026stable}
	Киселев~Н.\;С., Мешков~В.\;С., Грабовой~А.\;В. Устойчивая сходимость поверхности функции потерь через усреднение по распределению // Труды Института системного программирования РАН (электронный журнал). 2026. Т.~38, №~3. С.~71--82.

\bibitem{grabovoi2024unraveling748584228}
	Kiselev~N.\;S., Grabovoy~A.\;V. Unraveling the Hessian: A Key to Smooth Convergence in Loss Function Landscapes // Doklady Mathematics. 2024. Vol.~110, no.~S1. pp.~S49--S61. DOI:~10.1134/s1064562424601987.

\bibitem{grabovoy2025sample749476752}
	Kiselev~N.\;S., Grabovoy~A.\;V. Sample Size Determination: Likelihood Bootstrapping // Computational Mathematics and Mathematical Physics. 2025. Vol.~65, no.~2. pp.~416--423. DOI:~10.1134/s0965542524702002.

\bibitem{grabovoi2025sample727559461}
	Kiselev~Nikita, Grabovoy~Andrey. Sample size determination: posterior distributions proximity // Computational Management Science. 2025. Vol.~22, no.~1. pp.~1. DOI:~10.1007/s10287-024-00528-9.

\bibitem{motrenko2022numerical613055643}
	Grabovoy~A.\;V., Gadaev~T.\;T., Motrenko~A.\;P., Strijov~V.\;V. Numerical Methods of Sufficient Sample Size Estimation for Generalised Linear Models // Lobachevskii Journal of Mathematics. 2022. Vol.~43, no.~9. pp.~2453--2462. DOI:~10.1134/s1995080222120125.

\bibitem{grabovoi2022probabilistic609999418}
	Grabovoy~A.\;V., Strijov~V.\;V. Probabilistic Interpretation of the Distillation Problem // Automation and Remote Control. 2022. Vol.~83, no.~1. pp.~123--137. DOI:~10.1134/s000511792201009x.

\bibitem{grabovoi2021bayesian609999432}
	Grabovoy~A.\;V., Strijov~V.\;V. Bayesian Distillation of Deep Learning Models // Automation and Remote Control. 2021. Vol.~82, no.~11. pp.~1846--1856. DOI:~10.1134/s0005117921110023.

\bibitem{bakhteev2020vvedenie609999528}
	Грабовой~А.\;В., Бахтеев~О.\;Ю., Стрижов~В.\;В. Введение отношения порядка на множестве параметров аппроксимирующих моделей // Информатика и ее применения. 2020. Т.~14, №~2. С.~58--65. DOI:~10.14357/19922264200208.

\bibitem{bakhteev2019estimation609999169}
	Грабовой~А.\;В., Бахтеев~О.\;Ю., Стрижов~В.\;В. Определение релевантности параметров нейросети // Информатика и ее применения. 2019. DOI:~10.14357/19922264190209.

\bibitem{dorin2025za-chertoi807096471}
	Варламова~К.\;Д., Дорин~Д.\;Д., Грабовой~А.\;В. За чертой знакомых доменов: исследование обобщающей способности детекторов машинно сгенерированных изображений // Доклады Российской академии наук. Математика, информатика, процессы управления. 2025. Т.~527, №~7. С.~103--116. DOI:~10.7868/S2686954325070094.

\bibitem{dorin2025uluchshenie807096862}
	Дорин~Д.\;Д., Грабовой~А.\;В., Стрижов~В.\;В. Улучшение декодирования данных ФМРТ с использованием пространственно-временных характеристик в условиях ограниченного набора данных // Доклады Российской академии наук. Математика, информатика, процессы управления. 2025. Т.~527, №~7. С.~11--30. DOI:~10.7868/S2686954325070021.

\bibitem{gritsay2024stack748584235}
	Gritsai~G.\;M., Khabutdinov~I.\;A., Grabovoy~A.\;V. Stack More LLM’s: Efficient Detection of Machine-Generated Texts via Perplexity Approximation // Doklady Mathematics. 2024. Vol.~110, no.~S1. pp.~S203--S211. DOI:~10.1134/s1064562424602075.

\bibitem{dorin2024forecasting716107264}
	Dorin~Daniil, Kiselev~Nikita, Grabovoy~Andrey, Strijov~Vadim. Forecasting fMRI images from video sequences: linear model analysis // Health Information Scient and Systems. 2024. Vol.~12, no.~1. pp.~55. DOI:~10.1007/s13755-024-00315-5.

\bibitem{gritsay2024artificially638338385}
	Gritsay~G.\;M., Grabovoy~A.\;V., Kildyakov~A.\;S., Chekhovich~Yu\;V. Artificially Generated Text Fragments Search in Academic Documents // Doklady Mathematics. 2024. DOI:~10.1134/s1064562423701211.

\paragraph{Материалы международных конференций, индексированные Scopus и/или Web of Science}

\bibitem{grabovoi2024convnets743111032}
	Meshkov~V., Kiselev~N., Grabovoy~A. ConvNets Landscape Convergence: Hessian-Based Analysis of Matricized Networks // 2024 Ivannikov Ispras Open Conference (ISPRAS). 2024. Pp.~1--10. DOI:~10.1109/ispras64596.2024.10899113. IEEE.

\bibitem{bakhteev2022anti-distillation609999885}
	Petrushina~K., Bakhteev~O., Grabovoy~A., Strijov~V. Anti-Distillation: Knowledge Transfer from a Simple Model to the Complex One // 2022 Ivannikov Ispras Open Conference (ISPRAS). 2022. DOI:~10.1109/ispras57371.2022.10076855. IEEE.

\bibitem{gritsay2025advacheck734353374}
	Gritsai~G., Voznuyk~A., Khabutdinov~I., Grabovoy~A. Advacheck at GenAI Detection Task 1: AI Detection Powered by Domain-Aware Multi-Tasking // Proceedings of the 1st Workshop on GenAI Content Detection (GenAIDetect). 2025. Pp.~236--243. International Conference on Computational Linguistics, Abu Dhabi, UAE.

\bibitem{grabovoi2024n-gram743111061}
	Poimanov~D., Mestetsky~L., Grabovoy~A. N-Gram Perplexity-Based AI-Generated Text Detection // 2024 Ivannikov Ispras Open Conference (ISPRAS). 2024. Pp.~1--8. DOI:~10.1109/ispras64596.2024.10899150. IEEE.

\bibitem{gritsay2024multi-head682821438}
	Gritsai~G., Khabutdinov~I., Grabovoy~A. Multi-head Span-based Detector for AI-generated Fragments in Scientific Papers // Proceedings of the Fourth Workshop on Scholarly Document Processing (SDP 2024). 2024. Pp.~220--225. DOI:~10.18653/v1/2024.sdp-1.21. Association for Computational Linguistics, Bangkok, Thailand.

\bibitem{boeva2024team716107356}
	Boeva~G., Gritsay~G., Grabovoy~A. Team ap-team at PAN: LLM Adapters for Various Datasets // Working Notes of the Conference and Labs of the Evaluation Forum (CLEF 2024). 2024. Vol.~3740 (CEUR Workshop Proceedings). Pp.~2527--2535. CEUR-WS.org.

\bibitem{gritsay2022automatic526652068}
	Gritsay~G., Grabovoy~A., Chekhovich~Y. Automatic Detection of Machine Generated Texts: Need More Tokens // Ivannikov Memorial Workshop Proceedings 2022. 2022. DOI:~10.1109/ivmem57067.2022.9983964.

\paragraph{Публикации в других изданиях}

\bibitem{grabovoi2025o-slozhnosti805932780}
	Грабовой~А.\;В. О сложности моделей и данных в задачах обучения нейросетевых моделей // Тезисы докладов 22-й всероссийской конференции с международным участием {\flqq}Математические методы распознавания образов (ММРО-2025){\frqq}, Муром, 22–26 сентября, 2025 г. 2025. С.~72–73. ООО {\dq}МАКС Пресс{\dq}, Москва.

\bibitem{grabovoi2025skhodimost805932360}
	Мешков~В.\;С., Киселев~Н.\;С., Грабовой~А.\;В. Сходимость ландшафта сверточных нейронных сетей: анализ матричных сетей на основе матрицы Гессе // Труды 67-й Всероссийской научной конференции МФТИ. 2025. (Прикладная математика и информатика). Физматкнига.

\bibitem{grabovoi2025skhodimost805932320}
	Киселев~Н.\;С., Грабовой~А.\;В. Сходимость поверхности функции потерь как признак достаточного размера выборки // Труды 67-й Всероссийской научной конференции МФТИ. 2025. (Прикладная математика и информатика). Физматкнига.

\bibitem{grabovoi2025dostatochnyi805932760}
	Киселев~Н.\;С., Мешков~В.\;С., Грабовой~А.\;В. Достаточный размер обучающей выборки и его связь со сходимостью поверхности функции потерь // Тезисы докладов 22-й всероссийской конференции с международным участием {\flqq}Математические методы распознавания образов (ММРО-2025){\frqq}, Муром, 22–26 сентября, 2025 г. 2025. С.~73–76. ООО {\dq}МАКС Пресс{\dq}, Москва.

\bibitem{grabovoi2024opredelenie716107458}
	Киселев~Н., Грабовой~А. Определение достаточного размера выборки по апостериорному распределению параметров модели // Труды 66-й Всероссийской научной конференции МФТИ, 1–6 апреля 2024 г. 2024. С.~141. ISBN~978-5-89155-414-6. (Прикладная математика и информатика). Физматкнига.

\bibitem{gritsay2025neyavnaya805932701}
	Грицай~Г.\;М., Грабовой~А.\;В. Неявная регуляризация векторного представления текстов в подходе многозадачного обучения // Тезисы докладов 22-й всероссийской конференции с международным участием {\flqq}Математические методы распознавания образов (ММРО-2025){\frqq}, Муром, 22–26 сентября, 2025 г. 2025. С.~78–79. ООО {\dq}МАКС Пресс{\dq}, Москва.

\bibitem{gritsay2025vyravnivanie805932244}
	Грицай~Г.\;М., Грабовой~А.\;В. Выравнивание представлений в подходе многозадачного обучения для детектирования машинно-сгенерированных текстов // Труды 67-й Всероссийской научной конференции МФТИ. 2025. (Прикладная математика и информатика). Физматкнига.

\bibitem{bayazitov2022distillyatsiya613062660}
	Баязитов~К.\;М., Грабовой~А.\;В., Стрижов~В.\;В. Дистилляция моделей глубокого обучения на многодоменных выборках // Интеллектуализация обработки информации: Тезисы докладов 14-й Международной конференции. 2022. С.~98–99. ISBN~978-5-907366-77-0. Российская академия наук, Москва.

\bibitem{dorin2025dekodirovanie805932621}
	Дорин~Д.\;Д., Киселев~Н.\;С., Грабовой~А.\;В. Декодирование визуальных стимулов из мультимодальных сигналов мозга // Тезисы докладов 22-й всероссийской конференции с международным участием {\flqq}Математические методы распознавания образов (ММРО-2025){\frqq}, Муром, 22–26 сентября, 2025 г. 2025. С.~124–126. ООО {\dq}МАКС Пресс{\dq}, Москва.

\bibitem{dorin2025uluchshenie805932300}
	Дорин~Д.\;Д., Грабовой~А.\;В. Улучшение декодирования фМРТ в условиях ограниченной выборки // Труды 67-й Всероссийской научной конференции МФТИ. 2025. (Прикладная математика и информатика). Физматкнига.

\end{thebibliography}
\end{document}